# 需求：登记面与判据面余项 —— 批 13

- 日期：2026-09-15
- 设计输入：[`2026-09-15-registry-criteria-consult-minutes.md`](./2026-09-15-registry-criteria-consult-minutes.md)（会诊 id 2，**4/4 交付**；§1 父侧实测 13 条 · §2 四问裁定 · §3 D12-13…D12-16）
- 用户裁定（范围，已锁定）：批 13 = 交接页 §4 的 **R-9 · R-8 · R-6 · R-5 · R-4 · O-E5 · R-25**；**若触碰 `lib/**`，D-38 同批收掉**；**R-6 的遗留 `.thincoder/advisor.md` 探测链 = 保留，注释标 legacy**
- 章节：§0 会诊汇总 · §1 总目标 · §2 背景与问题 · §3 用户故事 · §4 非功能标准 · §5 边界与登记面
- 状态：**已实施（批 13）** —— 设计评审轮次 1 `VERDICT: PASS`（🔴0 🟡5 🔵5，十条全采纳）→ 实施（九 stage）→ **独立分歧审计 🔴1 🟡4 🔵5**（含一处**修完比原来更糟**的 128 容忍面）→ **设计修订（D13-19/D13-20）** → 修复轮 → **交付代码复评轮次 1 = `VERDICT: FAIL`（🔴1：同一机制在本批两份文档里有两套互斥规格）** → **父侧同步四处副本 + 复评轮次 2**。落档见设计档 §13

---

## §0 会诊汇总

**全量纪要见 [`2026-09-15-registry-criteria-consult-minutes.md`](./2026-09-15-registry-criteria-consult-minutes.md)**。

### §0.1 模型结果（**4/4 交付**）

| 模型 | 关键贡献 |
|---|---|
| `codex-cli:gpt-6-astra` | R-6 独立键 · **R-25「纯函数 + 内存虚拟文件名」⇒ 不新增顶层 `test(`** |
| `zai-coding-cn:glm-5.3` | **★ 勘误 R-5 前提** · **★ O-E5 的更坏失效链（冻结侧 fail-open）** · **R-9 与 R-6 必须同一次编辑** |
| `deepseek-official:deepseek-v4-pro` | **★ 独立勘误 R-5 前提** · O-E5 双层 fail-closed 论证 · R-6 六面清单与分界 · R-25 双向差集 |
| `kimi-api:kimi-k3` | **★★ 闸侧拿不到会话身份（决定性）** · **R-25 必须解析 README 全文** · 豁免恰一条 |

### §0.2 父侧实测（13 条全在纪要 §1；此处只列决定设计的八条）

| # | 事实 |
|---|---|
| 1 | **R-9 缺 4 项 + 2 个文件**（登记只说 1 键 1 文件）：缺 `contextTokens` · `standardsDoc` · `documentMapDoc` · **组字段 `runner`**；且 `README.md:169-172` 是同构第二处 |
| 2 | **R-4a 是 3 处同类**（`:881` / `:955` / `design-review-guard:773`）⇒ **没有 `.git` 的副本里整份套件部分转红** |
| 3 | **R-5 前提勘误（四家一致）**：6 维句**只在 design round-1 发出**（`:308`→`:349`；code 走 `:352`，判据来自 `:376` 的 5 条内置） |
| 4 | **O-E5 真实失效链**：基分叉时 **hash 侧 fail-closed**（可用性损伤）· **冻结侧 fail-open**（**保护性损伤**） |
| 5 | **★ 闸侧拿不到会话身份**：`makeDocFreezeGate` 注册为**全局** `tools/pre-execute`，闭包只吃 `getFreezeSet` ⇒ 签名分叉方向**要么不可行、要么造出两侧基不同的劈裂** |
| 6 | **R-25 域**：`docs/` 顶层 **43** 个 `.md`、**无子目录**；已登记 **42**、唯一未登记 = `README.md` 自身 ⇒ **落地即绿、零追加豁免** |
| 7 | **R-25 匹配口径必须是 README 全文**（`handoff.md` 走 bullet，只解析两表会**误报**） |
| 8 | **D-38 两处注释都在字节锁区间之外** ⇒ 修它**无需重基线夹具** |

### §0.3 父侧裁定（详见纪要 §2/§3）

| # | 裁定 |
|---|---|
| **J13-1** | **R-5** = 在 `advisor-msgs.mjs:328` **原地追加第 7 维**，**无条件、不条件化**（分支本身就是条件）、**不从系统提示收掉**；措辞保留 **when-present 对冲** |
| **J13-2** | **O-E5** = **本批不修，只订正登记**；订正三要素（已证事实 / 残余风险 / **修法方向预授权 = 入口边界按会话 cwd 预解析**，前置条件 = DSH `pre-execute` 载荷的会话身份核验） |
| **J13-3** | **R-6** = 新增**独立**声明键 `advisor.criteriaDoc`，**严禁并入** `standardsDoc` 家族；**六面**最小可交付；**遗留探测链保留并标 legacy**（用户裁定） |
| **J13-4** | **R-25** = 落 `test/doc-hygiene.test.mjs`，**并入既有块**（不开新顶层 `test(`）⇒ 台账零改；域 = `docs/` 顶层非递归；例外恰一条 |
| **J13-5** | **R-9 与 R-6 的注释修正必须同一次编辑**（否则修完 R-9 原地再造一个 R-9） |
| **J13-6** | **R-4a 三处同类一并治**（不止登记说的那一处）；修法镜像**锚 B 的容忍形态** |
| **J13-7** | **R-4b** = 删 `:293` 死代码 |
| **J13-8** | **D-38** = 两处陈旧活句同批订正（`:52` →「可执行行逐字节」；`:471-472` → 点名 `doc-hygiene.test.mjs:325-330` 与「区间不重叠」） |
| **J13-9** | **父侧自己的表述订正**：O-E5 的「机制内部自洽 ⇒ 无缺陷」是**错的**——`自洽 ≠ 保护` |
| **J13-10** | **R-8** = `lib/index.mjs:960` 的示例泛化，去掉本仓形状 |

---

## §1 总目标（一句话）

把交接页 §4 里**七条登记了很久、无阻塞、且经实测确认「东西还在」**的余项一次收掉——**其中三条的登记描述本身是错的，本批把「登记文本订正」列为与代码修订同级的交付物**。

---

## §2 背景与问题

| # | 问题 | 根因 | 实测 |
|---|---|---|---|
| **P1** | **白名单注释落后三个版本（4 项 / 2 文件）** | 批 5 与批 7 都把 `cordis.patch.yml` 划出写域 | 而注释正是**用户配那两个键时的唯一指引** |
| **P2** | **一条判据在用户消息里缺列** | 系统提示 7 维、用户消息 6 维 | 两条清单**都不被任何锁覆盖** |
| **P3** | **没有 `.git` 的副本里部分套件转红** | `code = undefined`（`status = 128`）挡不住 `ENOENT` 判据 | **3 处**同类 |
| **P4** | **评分链里有不可达死代码** | `\|\| dLen === 0` 使循环末轮必返 | 死代码在 provenance 模块里**比删除更危险** |
| **P5** | **登记把 O-E5 说反了** | 描述的是「形态错配」，真实是「**基分叉下的错根锚定**」 | **冻结侧 fail-open = 保护性损伤** |
| **P6** | **两条陈旧活句指向已变/不存在的锁** | 批 12 收窄锁面后未回头改 `lib/**` | 受 N-1 冻结，**本批触发条件成立** |
| **P7** | **「新建文档必须登记」无机检** | 纯人眼闸 | 地图**当前 42/43 完整** ⇒ 是**规则无契约**，不是已漂移 |
| **P8** | **示范路径带本仓形状** | 无 | 与「提示词不得指涉本仓」同族 |

**靶心 = P1/P7**（用户直接受影响的摩擦 + 规则缺契约）；**P5 是本批唯一的「发现比登记更坏」**。

---

## §3 用户故事

| # | 用户故事 | 判定句（可机验） |
|---|---|---|
| **US-1** | 作为**配置用户**，我希望白名单注释与判据**一致**，不要让我配到一半发现少键 | 注释列出的键集 == `index.mjs` 的 `topAllowed` + advisor 子键集 + 组字段集；**两文件**（`cordis.patch.yml` + `README.md`）同改 |
| **US-2** | 作为**评审员**，我希望 design 评审收到的两份清单**维度一致** | design round-1 用户消息**含** `document ownership`；**code round-1 不含**（负向锚） |
| **US-3** | 作为**在无 `.git` 副本里跑测试的人**，我希望套件**不因环境转红** | 三处 git 交叉核验在 **`status === 128` 且 stderr 命中 `/not a git repository/`** 时**warn + skip**（镜像锚 B）；**ENOENT 仍 skip**；**其余错误（含 `bad revision` 与 `path '…' does not exist in 'HEAD'` 两种 128）一律 `throw`** —— **★ 复评 #1 订正**：本行初写「在 `code === undefined` 且 `status === 128` 时 warn + skip」，**无 stderr 条件**；照那字面实现，会把「该 path 在该 revision 不存在」（**真回归：文件被改名/删除**）也降级成静默 warn ⇒ **比本条要治的原问题更糟**（见设计档 **D13-19**） |
| **US-4** | 作为**维护者**，我希望 provenance 模块里**没有死代码** | `deathLine` 尾部不可达 `return` 已删；行为逐字节不变（既有断言全绿） |
| **US-5** | 作为**审计员**，我希望 O-E5 的登记**说对**，且**正确的修法方向被预先授权** | 登记文本三要素齐（已证事实 / 残余风险 / 修法方向 + 前置条件）；**不得**再写「自洽 ⇒ 无缺陷」 |
| **US-6** | 作为**配置用户**，我希望能用**配置键**指定 code 评审的判据档，而**不再依赖上游产品名路径** | `advisor.criteriaDoc` 三态生效（可读 ⇒ 注入 / 不可读 ⇒ **响亮句点名键** / 未声明 ⇒ legacy 链）；PUT 走 `isDocPath`；**merge 白名单**含该键 |
| **US-7** | 作为**存量用户**，我希望键化**不改变我今天的任何行为** | 未声明时**逐字节**保持今日链（探 `.thincoder/advisor.md` → `DEFAULT_CRITERIA`）；该链**在注释里标 legacy** |
| **US-8** | 作为**下一批的维护者**，我希望**新建文档漏登记**会被机器抓住 | 域内每个 basename 必在 README **全文**的 `](name)` 链接里；例外**恰一条**（README 自身）；**落地即绿** |
| **US-9** | 作为**审计员**，我希望那条新机检**能被证明会失败**（非恒真锁） | 谓词自证：注入合成未登记名 ⇒ 必红；空登记簿 ⇒ 必红 |
| **US-10** | 作为**审计员**，我希望 `lib/**` 里的陈旧活句**指向真实存在的东西** | `:52` 说「**可执行行**逐字节」；`:471-472` 点名 `doc-hygiene.test.mjs:325-330` 并写明「**区间不重叠**」 |
| **US-11** | 作为**审计员**，我希望示范路径**不带本仓形状** | `lib/index.mjs:960` 的示例不含 `lib/x.mjs` |
| **US-12** | 作为**维护者**，我希望本批**不引入新的 doc-code 漂移** | 台账 §三 **零改**（不新增顶层 `test(`）；六串锁零命中；`failStop(` 计数仍 18；`path-kind.test.mjs:321` 的「卡片恰两文本框」口径**须同步**（因本批加第三键） |

---

## §4 非功能标准

| # | 标准 | 判定 |
|---|---|---|
| **N-1** | **不新增测试档** | `readdirSync(test)` 的 `.test.mjs` 数不变 ⇒ T-E19 / T-LC1 / T-LC4 零触发 |
| **N-2** | **不新增顶层 `test(`** | 全部新断言并入既有块 ⇒ 台账 §三零改、T-LC2 零触发 |
| **N-3** | **基线 10 档零编辑**（除授权面） | `git ls-tree 2e6ca8b -- test` 的档里只有 `design-review-guard.test.mjs` 被改，而它**已在 `AP_TEST_AUTHORIZED`** |
| **N-4** | **`.txt` 夹具零触碰** | 受锚 B + 口径锁双重保护 |
| **N-5** | **`lib/**` 六串零命中**（T-PK15） | 新写的注释与措辞不得引入那六串 |
| **N-6** | **`failStop(` 计数恒 18** | `doc-hygiene.test.mjs:319-324` |
| **N-7** | **`stripComments` 实现逐字节不动** | 批 12 的锁面基线 |
| **N-8** | **既有 453 用例全绿** | 本批**零新增用例数** |

---

## §5 边界与登记面

### §5.1 本批不做

| # | 不做项 | 理由 |
|---|---|---|
| 1 | **修 O-E5** | **闸侧拿不到会话身份**（kimi 决定性发现）⇒ 签名分叉方向要么不可行、要么造出两侧基不同的劈裂；属**守卫 E 机制变更**，另立批次（**修法方向已预授权**） |
| 2 | **退役 `.thincoder/advisor.md`** | **破坏性**（存量部署静默失去判据）；**用户裁定保留 + 标 legacy** |
| 3 | **把 `criteriaDoc` 接进 design 评审** | design 判据住系统提示，另一条链 |
| 4 | **改 `DEFAULT_CRITERIA` 内容** | 零收益 |
| 5 | **设置页卡片结构性重构** | 只加第三个输入框 |
| 6 | **给判据注入加预算** | 遗留链今日无预算——**登记为后续项** |
| 7 | **会话级覆盖** | J10 已裁：项目属性 global-only |
| 8 | **R-25 的反向腿**（已登记链接悬空） | 另一缺陷物种——**登记为后续项** |
| 9 | **把 R-25 落发布门** | G5 号段机锁保留 + 「七闸」口径三处 ⇒ **反而制造本批要治的漂移** |
| 10 | **改 `cordis.patch.yml` 的 base 专属段语义** | 只订正注释文本 |

### §5.2 登记面

| # | 级别 | 内容 |
|---|---|---|
| **R-33** | 🔵 | **「登记说错了」第三次同物种**（前两次：批 12 的 `prompt-common-consult-minutes.md:98`、本批的 O-E5 与父侧 R-5 简报）——三条都是**对机制的事实描述**出错，**且三条都无机锁覆盖** ⇒ **本批把「登记文本订正」列为与代码修订同级的交付物** |
| **R-34** | 🔵 | **`self-consistency ≠ protection`**——O-E5 的教训：两侧同函数同基只证明「一致地锚在同一个根」，**不证明那个根是对的**。⇒ 凡「两侧共用同一点」的论证，**必须另问「那个点本身对不对」** |
| **R-35** | 🔵 | **全局钩子拿不到会话身份**——`makeDocFreezeGate` 注册为全局 `tools/pre-execute`，闭包只吃 `getFreezeSet` ⇒ **任何需要「按会话」的判据在此形态下都无法实现**。这是 O-E5 修法方向的**结构性前置条件**，登记为**跨批次约束** |
| **R-36** | 🔵 | **判据注入无预算**（遗留链）· **R-25 反向腿**（链接腐烂）——两项均登记为后续项，各有独立触发 |

---

## §6 历史行

| 日期 | 变更 |
|---|---|
| 2026-09-15 | 首版（设计待评审）：会诊 id 2 **4/4 交付** → §0 汇总 + 八条决定性实测 + **J13-1…J13-10 裁定**（含用户对 R-6 遗留链的裁定）；US-1…US-12；N-1…N-8；§5.1 **十项不做** + §5.2 **R-33…R-36** |
| 2026-09-15 | **设计评审轮次 1 修正块（`VERDICT: PASS`）**：#1 订正要求「G9 的验收面补 AC-22」（原止于 AC-21）；附录 §0.2 与 §5.1 的对应项同步。 |
| 2026-09-15 | **交付期修订块**：**D13-19**（🔴 收紧 `status === 128` 的容忍——原「只看 status」会把「path 在该 revision 不存在」这种**真回归**降级成静默 warn）；**D13-20**（两处硬编码两键换 `PK_KEYS`——其中 T-PK14d 是 **AC-13 的唯一覆盖点**）；**US-3 判定句随之订正**（原写「`code === undefined` 且 `status === 128` 时 warn + skip」，**无 stderr 条件**）；§5.2 增 **R-39**。 |
| 2026-09-15 | **复评轮次 1（`VERDICT: FAIL`，🔴1）修正块**：评审指出**同一机制在本批两份文档里有两套互斥规格**——收紧后的谓词已写进设计档 **§4 D13-19 / §11.3 stage 2**，但 **§8.2 V4 / §11.2 AC-5 / §13.2-b V4 / 本档 US-3** 四处**副本未同步**。**父侧认并同步四处**。**★ 这是本批「规格不追列表」第三次打中本批自己**（前两次：AC-22 被设计评审 #1 抓出 · 边界 10 的「家族四面自动扩」被审计 F3 证伪）。 |
