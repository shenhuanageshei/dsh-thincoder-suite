# 缺陷登记表：dsh-thincoder-suite 机制缺陷治理

- 日期：2026-09-05（thorough 审计定稿，审计子代理报告全量合并）
- 状态：LIVING（随修复轮推进更新 status）；**R0-R6 全部交付且真机验证收口（2026-09-05/09）——2026-09-11 增补 D-30（design token 生命周期）；2026-09-12 增补 D-31…D-34；2026-09-13 增补 D-35…D-37；本表登记 **36** 条：已修 **34**〔D-01…D-07 / D-09…D-28 / **D-30** / **D-31** / **D-33** / **D-34** / **D-35** / **D-36** / **D-37**〕⊕ 已驳回 1〔D-08，附平台 retainTail 证据〕⊕ **号段保留 1**〔D-32，未使用的编号占位〕⊕ **待修 0**（批 10 收口 D-31 + D-36）**（D-25 已修但结论被 D-30 反转——见其行内反转标注；D-29 配置面缺口登记于 CHANGELOG 与批 1 CHANGELOG 条目，未单列本表）。**2026-09-13 计数订正（批 10）**：本表此前自报「33 条」，而**机械数出的表体数据行 = 36** ⇒ 自报值已订正为 **36**（同时补入此前漏登的 **D-35**——它早已由批 4 交付，只在 :4 行的已修枚举里漏了）；**批 10 组成写全** = 台账纪律（可落地版：计数有据 + 指针/触发字段）+ **D-31** + **D-36** + 文档卫生。**当前余项 = 0**；本行口径与吸收清单 §6、交接页 §1/§4 一致。**计数纪律（R-19）**：自报计数**单点**、他处用指针——本行是「本表条数」的唯一自报点
- 证据基线：git `e4aae98`，全部 file:line 已按当前工作树核对
- 图例：风险 🔴 阻塞级 / 🟡 重要 / 🔵 次要；状态 待修 / 修复中 / 已修 / 已裁决处置 / 已驳回 / 待测量
- 轮次：R1 诊断与校验层 / R2 执行架构与进程 / R3 回落与失败语义 / R4 一致性与 UX 收口（轮次定义见设计文档）

## 登记表

| ID | 主题 | 现象（一句话） | 根因状态（含证据） | 风险 | 轮次 | 状态 | 指针 | 触发 |
|---|---|---|---|---|---|---|---|---|
| D-01 | 失败语义 | advisor dsh 空响应不可诊断：两种形态坍缩为同一句文案（finish===null / stop 零块），finish.reason 从不记录；空响应不重试、不进任何计数 | 已定位 advisor.mjs:779（审计修正：坍缩形态实为 2+1，error-无-message 有独立路径但同样无归因）。**2026-09-05 生产复现 ×2**：① glm-5.3/max；② deepseek-v4-pro/high——跨模型复现指向结构性根因：LLM_MAX_TOKENS=8192（advisor.mjs:57 硬编码）在推理阶段耗尽 → finish=length 零文本块 → 代码不检查 length 落入坍缩路径（finish.kind 无记录，推断待 R1 观测证实） | 🔴（升级：两次阻塞设计评审→阻塞 token 签发→阻塞实施链） | R1 观测 + R3 重试/分类（D-裁决-1）；修复应含 finish=length 显式分类、maxTokens 不足的独立诊断、**maxOutputTokens 可配置化**。〔2026-09-05 用户授权热修：LLM_MAX_TOKENS 8192→16384 一行（工程模式例外，留痕于此）；R1 正式化〕 | **已修**（R1 观测 + R3 重试/分类全量。R1 交付：三形态+length 分类行、stream observation 留档、codex 信封 usage 上浮、advisor.maxOutputTokens 配置化。R3 交付（D-裁决-1）：非基础设施形态空响应〔finish-null/stop 零块/length/非预期形态〕自动重试一次〔复用 messages、与 stall 通道独立计数、有产出轮重置资格、重试不豁免预算〕；再空 → "Advisor: review failed (empty response — 重试一次仍空)" 前缀可归因失败〔分类+观测+重试标记；不烧轮次不写 prior〕；基础设施形态〔error 无 message / stall〕走既有失败路径不空重试；回落轮空响应重试后仍空按回落失败计（D-07 连败链）。〔勘误：设计 §5.2 所指「现状空响应被 finalize 当 completed 烧轮次」经 probe 复核不成立——自基线 e4aae98 起空响应占位文本即带 "Advisor:" 前缀，R3 系将占位措辞收敛为显式失败语义（措辞修正而非行为修正）；R1 空响应 3 用例断言随新语义更新〕） | lib/advisor.mjs | —（已闭环） |
| D-02 | 能力校验 | effort 校验漏点实为矩阵级：consult:165 / escalate:207 裸传；eng:399 codex 用原始值且 :385 按父模型解析（对 codex 无意义）；全部 codex 行无 L1（catalog 数据存在但从未用作校验源）；effort-resolve.mjs:6 「四点同修」注释为假 | 已定位（消费点×层矩阵见审计 §4，9 行中 6 行 GAP） | 🔴 | R1（D-裁决-2 最近支持档） | **已修**（2026-09-05 R1 交付：resolveSupportedEffort 重规约最近支持档/等距向上取 + 新增 resolveCodexRowEffort〔codex catalog 数据源〕+ 接线 6 漏点〔consult dsh/codex、escalate dsh/codex、eng codex、advisor codex runner〕+ eng dsh 解析移回 dsh 分支 + effort-resolve.mjs 注释修正 + 19 回归用例〔原记 17，审计更正为 19〕）。**遗留尾部**：consult codex 行的 row.effort 仍为死配置（运行时只消费 runner.effort）——架构裁决：R2 接通（镜像 advisor 的 row.effort → resolveCodexRowEffort → runner.effort 链）。**2026-09-05 R2 交付已接通**（requested = runner.effort ?? row.effort → resolveCodexRowEffort → 写回 runner.effort；runner.effort 优先级与目录回落用例各一——D-02 至此全量收口） | lib/effort-resolve.mjs | —（已闭环） |
| D-03 | 执行架构 | eng_coder codex 路径完全无钳制无告警：默认 30min 预算在 600s 墙钟下静默死亡 | 已定位 eng.mjs:402-411 | 🔴 | R2（jobs 迁移） | **已修**（2026-09-05 R2 交付：eng_coder codex 迁 ctx.jobs——预算 > budgetCap 且 jobs 可用 → 后台派发〔eng-codex job：owner 绑定 agent、cancel 走 ctrl.abort、run() 内全预算 + 自有 watchdog〕，立即返回 job 句柄 + UI-2 接续指令；≤ cap 或 jobs 缺失/派发抛错 → 同步钳制〔默认 30min → budgetCap〕+ 响亮告警；交付簿记移入 done 成功分支〔D-20 一并修〕） | lib/eng.mjs | —（已闭环） |
| D-04 | 执行架构 | escalate followup 绕过 budgetCap：runner.timeoutMs ?? 600000 = 零余量撞墙 | 已定位 escalate.mjs:101 + adapter:506 | 🔴 | R2 | **已修**（2026-09-05 R2 交付：followup 预算与首次同链解析〔runEscalateCodex 统一入口：runner.timeoutMs → cliCfg.defaultTimeoutMs → 600000 → budgetCap 钳制 / jobs 派发判定〕——不再把 runner.timeoutMs 原样透传；followup effort 复用已解析 runner 的语义由回归用例锁死〔R1 审计 🔵5〕） | lib/escalate.mjs | —（已闭环） |
| D-05 | 执行架构 | jobs 派发 job id 恒显 "?"：平台 start 返回 branded string，插件取 .id 落空；测试 fake 返回对象掩盖本缺陷 | 已定位 advisor.mjs:1072；平台 dsh-jobs-local/lib/index.js:176 `return id`。**2026-09-05 生产复现**：本次设计评审派发返回 "background job ?" | 🟡 | R2 | **已修**（2026-09-05 R2 交付：三处派发点 started 按 branded string 直接渲染〔escalate/eng 经共享 codexJobsDispatchReply、advisor 内联同语义〕，异常形态回落 "?" 而非崩溃；全部测试 fake 改返回 string——掩蔽消除，用例断言句柄真实渲染且不再恒显 "?"） | lib/advisor.mjs | —（已闭环） |
| D-06 | 并发安全 | advisor 后台评审无 single-flight：同会话二次调用照派第二个 job，双 finalize → 轮次双增、prior 踩踏 | 已定位（无 in-flight map；平台 10/owner 上限不是替代） | 🔴 | R2 | **已修**（2026-09-05 R2 交付：模块级在飞表〔advisor.mjs，复合键 sessionId+mechanism——每机制独立单飞槽位，同会话 advisor 在飞不阻塞 escalate/eng 派发〕；advisor/escalate/eng codex 派发前 checkInFlightJob，命中 → 拒绝并告知在飞 job id 与接续方式（本次未派发）；done settle〔成功/失败/中止两分支〕clearInFlightJob）。**审计观察 + 架构裁决（2026-09-05）**：同步路径（预算 ≤cap）未查在飞槽位——同机制 >cap job 在飞期间 ≤cap 调用并行执行，轮次/prior 仍可能双写；裁决=**扩展检查到全部入口（同步含）折入 R3**（US-4/N-3 per-mechanism 语义补全）。审计另注：done 回调在代际校验前变更 codexFailureCount——模块级计数器不在 sessionState 保护域、语义可辩护（运行确实发生），接受不改。**R3 交付（2026-09-05）**：advisor 机制全入口单飞——checkInFlightJob 提前到 runAdvisorReview 入口（在飞 >cap job 期间任何新调用含 ≤cap 同步调用/dsh 主路径调用被拒；派发点二次检查保留，防入口→jobs.start 之间 await 窗口内的他方占位竞态）。**范围缺口（上报待裁决）**：设计 §5.4 文字要求「三机制全部入口」，但 §5.5 受影响文件清单不含 eng.mjs 且 escalate.mjs 限定仅注释——escalate/eng 同步入口检查超出本轮授权文件清单，未实施（escalate/eng 的轮次/prior 双写面与 advisor 不同，缺口影响面见交付报告）。**R3 补丁轮交付（2026-09-05，设计 §5.5 勘误后清单已含两文件）**：escalate/eng 入口检查随补丁轮补齐——runEscalate（护栏之后）/runEngCoder（授权门禁之后）在机制入口、任何 spawn/runCodexTask 之前 checkInFlightJob：同机制在飞（>cap job 或任何已占位形态）时新调用被拒（R2 同款拒绝文本：Error 前缀含在飞 job id 与接续指引）；escalate 覆盖 followup 续轮/≤cap codex 同步/dsh 子代理路径，eng 覆盖 ≤cap codex 同步/dsh 子代理路径；codex jobs 派发点二次检查保留（入口→jobs.start await 窗口内他方占位竞态防护，镜像 advisor R3 主轮形态）；settle 双分支清除复用 R2 既有 clearInFlightJob（advisor.mjs checkInFlightJob/setInFlightJob 本就已导出，两机制直接复用零复制实现）。用例：escalate ≤cap 同步被拒（含 followup 续轮入口被拒）+ dsh 子代理路径被拒（llm/subagents 零触碰）+ settle 后恢复；eng ≤cap 同步被拒 + dsh 子代理路径被拒（subagents 零触碰）+ settle 后恢复（4 新用例，205/205 绿）。**D-06 至此三机制全入口收口** | lib/advisor.mjs | —（已闭环） |
| D-07 | 回落自愈 | 回落活锁：计数在回落**开始前**清零（:1015）；失败不推进 advisorRound → MAX_ADVISOR_ROUNDS 对失败序列完全失效，codex↔dsh 无界交替 | 已定位 advisor.mjs:1015/:942 | 🔴 | R3（D-裁决-3 连败 2 次硬停） | **已修**（2026-09-05 R3 交付，§5.1 精确计数器语义：codexFailureCount〔codex 失败 +1/任一路由成功清零/**delete 移到回落轮结果产出之后且仅成功时**——经 finalize completed 分支 resetRouteFailureState 单点收口；回落失败不清零 → 下一调用直接再进回落轮，不再 codex↔dsh 交替〕+ fallbackFailureCount〔回落轮失败 +1（含「回落路由不可达」形态）/任一路由成功清零〕+ 连续 2 次回落失败硬停〔"Advisor: 回落硬停"——双路由配置诊断：双路由各自状态 + 最近失败码/原因 + 修正指引（网络/代理、runner 切换、provider 检查）+ 解除方式〕；硬停 armed 后同会话后续 advisor 调用入口持续返回硬停指引（零 LLM/零 codex 活动，不烧轮次）——直到配置变更（advisorRouteFingerprint 路由指纹失配自动解除：advisor 组 ⊕ codexCli ⊕ 会话覆盖 ⊕ agent 路由）或会话重置（clearCodexFailureCount 扩展清双计数器/最近失败码/硬停，index.mjs D-22 挂点零改动）；任何零进度组合（含回落路由不可达、空响应回落失败）同经硬停终止；6 用例：活锁封顶（codex×2→回落×2→硬停）/硬停持续+配置变更解除/会话重置解除/delete-after-result 正反向/回落不可达组合/正向清零（验收⑥）） | lib/advisor.mjs | —（已闭环） |
| D-08 | 评审协议 | ~~design token 尾部截断丢失~~ **已驳回**：平台截断为保尾语义（retainTail），token 位于尾部反而安全；相邻真缺陷是 D-09 | 已驳回（dsh-tool-jobs/lib/index.js:100-107 TextRetainer kind:"tail"） | — | — | 已驳回（US-3 改为回归测试钉死该平台保证） | — | —（已闭环） |
| D-09 | 评审协议 | 派发文案谎称「通知含评审全文」——平台完成通知只含一行指针，全文必须 job_output 读取 | 已定位 advisor.mjs:1074 vs dsh-tool-jobs:116-132。**2026-09-05 生产复现**：本次派发返回文本即含该虚假承诺 | 🟡 | R2 | **已修**（2026-09-05 R2 交付：派发文案改指针+指引——共享 JOBS_CONTINUATION_INSTRUCTION〔「等待完成通知后再继续；长任务勿用 job_output wait 阻塞等待（平台等待上限 600s）——完成通知只含一行指针，全文经 job_output 读取」〕，三机制派发统一；「通知含评审全文」虚假承诺删除，用例断言旧文案不再出现） | lib/advisor.mjs | —（已闭环） |
| D-10 | 并发安全 | 晚到的 job finalize 复活已重置状态：无代际/版本检查，40min job 完成于 eng 交付重置之后会覆写 advisorRound/prior | 已定位 advisor.mjs:934-986 vs eng.mjs:415-422 | 🔴 | R2 | **已修**（2026-09-05 R2 交付：sessionState.advisorGeneration〔整数，随 F12 视图持久化——sessionStateViewWithGeneration 全写点统一拼入 + session-store normalizeRestored 条件白名单〕；eng 交付重置 / F11 类型切换 / apply-session 变更三处 bumpAdvisorGeneration；advisor jobs 派发时捕获代际、finalize 应用前校验——不匹配 → 丢弃状态变更（轮次/prior/token 不并入）+ 完成通知注明「状态代际已变更…本轮结果不并入（原文可读）」） | lib/advisor.mjs | —（已闭环） |
| D-11 | 进程卫生 | 写任务截断杀 = 半途编辑残留 + 无术后报告 + touchedFiles 丢失（partial 无 Touched 行）；jobs 迁移只护优雅销毁路径，宿主硬死孤儿仍在 | 已定位 adapter:626-635 + 硬死无 dispose | 🔴 | R2（告警+指引+清扫；硬死边界文档化） | **已修**（2026-09-05 R2 交付：① 失败/截断杀路径〔TIMEOUT/ABORTED/CLEANUP_FAILED〕返回文本加回滚指引〔「半途写入可能残留：对照 partial 输出与 Touched 提示检查工作区，必要时 git 回滚」〕+ partial 内 Touched 行 advisory 解析〔escalate/eng 共享 codexFailureAdvisory——提示可见但不并入审计范围〕；② 启动清扫 sweepStaleCodexTempDirs〔>24h 的 thincoder-codex-* 临时目录删除 + warn，index.mjs apply 启动接线〕；③ 进程级清扫维持文档化边界〔B9——无可靠属主标记，2026-09-05 二次评审确认接受〕） | lib/codex-adapter.mjs | —（已闭环） |
| D-12 | 设置页 UX | 保存竞态：busy 只禁按钮，表单可编辑；成功后 refreshView 整体替换草稿，保存期间编辑被静默覆盖 | 已定位 client.js:697/:552 | 🟡 | R4 | **已修**（2026-09-05 R4 交付，UI-3 双面防护：① busy 期间禁用**全部表单输入**（非仅按钮——六个输入组件增 disabled 透传，input/select/checkbox/池行删/添加/全按钮；`.tc-field:disabled` 样式可见）；② 保存成功后 refreshView 不整体替换——busy 窗口内（enterBusy 起算）触碰的字段保留用户值、未触碰字段重放新值 + 提示条「保存期间有编辑，已保留你的修改」（busyRef 同步镜像消除 state 闭包陈旧窗口；组字段派生重置一并记触碰维持 provider→model 联动不变式；池按整池保留〔行结构互相关联 + 保存侧净化可能重排索引〕；保留 codexCli/池字段时同步置 dirty 保证下次保存发出；restoreGlobal 同语义）。**验证形态**：本仓库无 client 测试面（浏览器 CJS bundle）——node --check 通过 + 逻辑代码级可读复核，登记如实标注（设计 §6.5 验收①交付形态勘误同步落盘）） | lib/client.js | —（已闭环） |
| D-13 | 配置一致性 | advisorOverride.runner 三面不对称：apply-session 接受、advisor_config 工具拒收、持久化白名单丢弃（重启即失）；client.js:726 注释与事实相反 | 已定位 advisor.mjs:84 / index.mjs:283-286 / session-store.mjs:84 | 🟡 | R4 | **已修**（2026-09-05 R4 交付，三面同步：① ADVISOR_OVERRIDE_GROUP_PATHS + advisor_config coerceValue 增 runner——校验走 normalizeRunnerValue〔字符串简写 "dsh"/"codex-cli" 或对象，归一化存储；"dsh" = 回落硬停修正指引 2 的显式回切通道〕，set/reset 路径用例 + 非法值拒收 N4 不变式用例；② session-store sanitizeAdvisorOverride 组字段白名单加 runner〔此前重启即失〕——往返用例：set runner → 落盘 → 模拟重启恢复 → 仍在且 resolveAdvisorRoute 继续生效；③ client.js applySession 注释修正〔原「sanitizeSessionAdvisor 不收 runner」与事实相反——真实语义为有意的 UX 取舍：runner 配置属全局面，会话级 runner 走 advisor_config〕，登记表 D-23 残项一并收口。apply-session 面一期已接受〔validateAdvisorGroup/sanitizeGroup〕零改动——三面全通。resolve 链一期即消费 runner〔resolveAdvisorRoute 组环合并含 runner〕，缺的只是入口与持久化两道白名单） | lib/session-store.mjs | —（已闭环） |
| D-14 | 进程卫生 | codex 全局并发无准入控制：consult ≤5 仅 per-consult，多机制叠加可 6+ 进程 | 已定位（缺失） | 🟡 | R2 | **已修**（2026-09-05 R2 交付：adapter 全局活跃计数 + 准入〔runCodexTask 包装层：检查与占位同一同步块、finally 释放〕，上限 codexCli.maxConcurrent〔DP-3 终裁默认 8，可配 1..64〕；超限 CONCURRENCY_LIMIT envelope fail-fast 并明确报错〔不排队——简单诚实〕；三面白名单同步〔index.mjs PUT 校验 ⊕ config-store merge 白名单 ⊕ resolveCodexCliGlobals 运行时解析〕+ 设置页字段 UI-4〕） | lib/codex-adapter.mjs | —（已闭环） |
| D-15 | 进程卫生 | idle 300s 默认值无健康流证据（研究文档附录的停滞都是网络退化；240s 是选择不是测量） | 已测量（2026-09-06 真机验证阶段完成） | 🔵 | R4（测量任务） | **已修（测量完成，结论=维持 300s 默认）**：3 次 high-effort 真实 codex 任务（读登记表选三条最险缺陷；wallMs 168237/203022/215274；事件行 16/14/16），**最大静默间隙 36.1s / 46.7s / 50.1s——全部 ≤240s 阈值**（判定规则 §6.3），健康流静默窗口对 300s 默认有 ~6 倍安全余量。测量方法：pwsh 时间戳逐行记录 codex exec --json 事件到达间隔（与插件消费的同一事件流）。结论：维持 idleTimeoutMs=300s 默认，无需上调或 effort 感知。 | lib/codex-adapter.mjs | —（已闭环） |
| D-16 | 执行架构 | budgetCap < maxWallMs 不变式只在文档：插件读不到 maxWallMs，运行时无检查无警告 | 已定位（平台唯一消费者 dsh-code-runtime-worker-thread:651/:919，无注入面） | 🟡 | R2（ mitigation：钳制时警告已有 + 设置页提示已有 + 派发时不变式提醒 + 标注为不可完全强制） | **已修**（2026-09-05 R2 交付〔mitigation 收口〕：派发时 budgetCapMs ≥ 600000 输出一次不变式提醒——共享 codexJobsDispatchReply〔句柄文本附带 + console.warn 留档：「确认已同步提高平台 run_code maxWallMs 且 budgetCap < maxWallMs；否则 jobs 缺失时的同步降级路径仍会在平台墙钟处被截断」〕；钳制告警与设置页提示此前已有；插件读不到 maxWallMs 的不可完全强制边界维持登记〔设计 §4.5〕） | lib/codex-adapter.mjs | —（已闭环） |
| D-17 | 执行架构 | advisor dsh 主路径 + 回落轮 timeoutMs 不钳制（最高 3.6M），与 codex 路径不对称 | 已定位 advisor.mjs:1023/:1119 | 🟡 | R1 | **已修**（2026-09-05 R1 交付：dsh 主路径 + 回落轮 effective=min(timeoutMs, budgetCapMs) + 结果尾部截断告警〔finalize 之后追加——插件元数据不污染 completed 判定/prior〕+ 钳制用例） | lib/advisor.mjs | —（已闭环） |
| D-18 | 失败语义 | 结构化观测缺失三件套：finish.reason / usage（codex 捕获了但从不上浮）/ blocks 计数；stderr 只留头部 4K（可用错误通常在尾部）；exit code 单独依赖 | 已定位 | 🟡 | R1 | **已修**（2026-09-05 R1 交付：advisor 流结束 stream observation〔finish.kind/failure.message/block 计数/usage〕console.warn 留档 + 并入失败/空响应文本；codex TIMEOUT/PROCESS_ERROR 诊断上浮 usage + TIMEOUT 信封补 usage 字段；stderr 保留改尾部 4K + 保尾断言。〔措辞核对（2026-09-05 R4 收尾，§6.4 ② 折入）：设计建议的收窄前提「仅 idle-watchdog 信封带结构化 usage，墙钟信封在诊断串内」与代码事实**不符**——墙钟 TIMEOUT 信封同样带结构化 usage 字段（codex-adapter.mjs 墙钟 watchdog 与 idle watchdog 两处 envelope 构造均传 usage，R1 `95af758` 交付；R1 用例 timeoutMs=300 + idle off 驱动的正是墙钟路径且断言 env.usage 深等）——按 §9 验收第 1 条「终态与代码一致」维持原措辞不收窄：§3.2 本就只要求诊断层，信封字段属超交付而非欠交付，收窄将引入与代码相反的登记〕） | lib/advisor.mjs | —（已闭环） |
| D-19 | 评审协议 | 成功路径后缀污染 prior：回落告警/effort note 拼进结果 → 存入 lastAdvisorOutput → 污染下一轮 prior 注入 | 已定位 advisor.mjs:1028/:1124→:941 | 🟡 | R3 | **已修**（2026-09-05 R3 交付，§5.3：finalize 入参收敛为纯评审正文——回落告警/effort note/截断提示三类机制性后缀全部移到 finalize **之后**追加（回落轮 / dsh 主路径 / codex 同步 / codex 同步降级 / jobs done 回调 5 处消费点）；lastAdvisorOutput 只存 body（appendCitationReport 仍属正文部分），返回文本照常带后缀（可见性不变）；looksLikeReview 200 字符阈值不再被机制后缀推过（短正文+长告警不再误进 prior）；5 用例：回落轮纯净/主路径纯净/codex 同步纯净/降级路径纯净/阈值修正） | lib/advisor.mjs | —（已闭环） |
| D-20 | 评审协议 | eng codex 失败路径仍执行交付簿记：重置轮次/置 mutatedThisRun/合并 touched——失败交付销毁评审预算并断言了可能不存在的变更 | 已定位 eng.mjs:413-422 在 ok-check 之前 | 🟡 | R2 | **已修**（2026-09-05 R2 交付：escalate〔touchedFiles 合并 + codexThreads 记录〕与 eng〔轮次重置/lastAdvisorOutput 清空/mutatedThisRun/touched 合并 + F12 落盘〕交付簿记全部移入任务完成回调的成功分支恰好一次；失败分支只输出诊断 + partial 的 advisory touched 提示；eng dsh 路径与 codex 同步降级路径同语义〔stopReason 非 completed 一律不簿记〕） | lib/eng.mjs | —（已闭环） |
| D-21 | 执行架构 | escalate/eng 的 **dsh 子代理路径**回合内无界等待（escalate:220 / eng:460 await run.result）；escalate.mjs:4 头注释「无墙钟」与平台事实矛盾 | 已定位 | 🔴 | R2（处置方案见设计文档 DP-1：clamp+warn，jobs 迁移另立项） | **已修**（2026-09-05 R2 交付〔DP-1 方案 A，已终裁全按推荐〕：escalate/eng dsh 路径 await run.result 加 budgetCap 内部截止〔AbortController + 定时器，父级 abort 经 forward 汇入同一控制器〕——到点 abort + 响亮告警〔「dsh 子代理路径受平台墙钟约束，超限任务被终止；如需长任务请走 codex runner 或拆分 stages」〕+ partial 输出；escalate.mjs:4 头注释「无墙钟」修正；dsh 路径 jobs 迁移另立项——方案 B 作废维持。〔**2026-09-06 修订**：方案 B 复活为 R5 全量三路径 dsh 后台化（用户修订终裁，见 D-27/设计 §7）；本条 R2 交付的钳制机制保留为 R5 后的同步快路径护栏〕 | lib/escalate.mjs | —（已闭环） |
| D-22 | 进程卫生 | 卫生批：codexFailureCount 会话销毁不清理；consult 子代理 run 从不 dispose；无孤儿启动清扫；cleanCwdRoot 死旋钮（消费但无处可设） | 已定位 | 🔵 | R2 | **已修**（2026-09-05 R2 交付：① codexFailureCount 会话销毁清理〔clearCodexFailureCount 挂 index.mjs session/disposed 既有清理组——与 clearCodexThread 同生命周期语义〕；② consult 子代理 run 补 dispose〔startConsultChild finally 统一释放，settle 成功/失败/中止皆 dispose〕；③ cleanCwdRoot 死旋钮删除〔resolveChildCwd 消费点与注释一并清——clean-cwd 一律一次性临时目录〕。孤儿启动清扫并入 D-11 交付〔sweepStaleCodexTempDirs〕） | lib/index.mjs | —（已闭环） |
| D-23 | 工程过程 | 版本 0.1.0 未锚定；文档-代码漂移标记 4 处（escalate:4 / advisor:1074 / client:726 / effort-resolve:6）；测试缺口（fake jobs.start 掩盖 D-05、single-flight/并发 finalize/eng codex 超限/followup 预算/runner 持久化往返零覆盖） | 已定位 | 🔵/🟡 | R1（版本+注释漂移）+ 随各缺陷回归用例 | **已修**（2026-09-05 R1 交付：版本锚定 0.7.0 + effort-resolve:6 注释修正 + R1 相关测试缺口补齐〔30 新增用例：effort 矩阵/观测/钳制/maxOutputTokens〕）；**R2 追加**（2026-09-05 R2 交付：escalate:4 头注释修正〔D-21〕、advisor 派发文案修正〔D-09〕、fake jobs.start 掩蔽消除〔D-05〕、测试缺口补齐——single-flight/代际 finalize/eng codex 超限/followup 预算各 ≥1 用例，R2 新增 30 用例〔原记 29，审计更正〕）；**R4 收口**（2026-09-05 R4 交付：client:726 注释修正〔D-13 一并——原「sanitizeSessionAdvisor 不收 runner」与事实相反〕+ 版本推进 0.8.0 + 登记表 24 条终态核对 + docs/README.md 三份新文档登记核验 ✓〔均在案〕——4 处漂移标记全部修正，D-23 全量收口） | CHANGELOG [0.8.0] | —（已闭环） |
| D-24 | 工程过程 | eng.mjs 从未经独立代码评审（两次环境阻塞）——评审欠账 | 已定位 | 🔵 | R4（补跑） | **已修**（2026-09-05 架构会话补跑完成：eng.mjs 独立 code review 通过——零 🔴，1🟡+5🔵 全部转入 D-26 后续维护轮） | lib/eng.mjs | —（已闭环） |
| D-25 | 安全 | TOKEN_SECRET 回落源码内公开默认值 `"thincoder-default-secret"`——默认部署下 design token（eng_coder 写门禁凭证）可被任何知悉默认值者伪造 | 已定位 advisor.mjs TOKEN_SECRET（thincoder 移植存量；R4 code review 🟡 发现） | 🟡 | R4 收尾微修复 | **已修**（2026-09-05 R4 收尾微修复轮交付：TOKEN_SECRET 解析链重规约〔advisor.mjs〕——优先级 ① env `THINCODER_TOKEN_SECRET`（空白串视为未设）→ ② `$DSH_HOME/.thincoder/token-secret` 持久化密钥〔复用 dsh-home.mjs 既有路径解析与 token-store 同链；缺失 → `crypto.randomBytes(32).hex` 生成 + writeFileAtomic 原子写 + chmod 0600 尽力而为；重启复用同一密钥〕→ ③ 无 env 且路径不可解析 → 回落公开默认值 + 响亮告警「design token 门禁使用公开默认密钥，不可信——设置 THINCODER_TOKEN_SECRET 或修复 DSH_HOME」。进程内模块级单例〔首次 token 操作时解析并缓存并保持稳定——刻意不在模块顶层急解析：宿主/测试进程对 env 的调整可能发生在 import 之后，顶层急解析会读到导入时刻的陈旧 env〕；持久化写失败 → 仍用已生成密钥 + warn（进程内稳定；重启重生成、在途 token 届时失效——诚实可见）。**既有在途 token 失效处理 = 无旧签名兼容通道**（保留旧默认验签 = 保留伪造通道，违背修复目的）：密钥切换后旧 token 因 HMAC 不匹配自然失效 → 重跑 design review 重铸〔TTL 1h 限制失效面〕；首次升级启动（公开默认 → 随机持久化）即发生一次，属修复预期代价。测试 4 用例：env 优先〔含空白串视为未设 + env 路径不写盘〕/ 生成 + 持久化 + 清缓存重解析〔模块级缓存模拟重启——在途 token 不因重启失效 + 换 DSH_HOME 负对照证缓存清零真实生效〕/ 无 env 无路径回落 + 响亮告警断言 / jobs 派发 warnPrefix 可见性〔本批 code review 跟进 #4 用例〕；215/215 绿。同批微修：escalate.mjs 死 import〔sessionState〕移除、client.js saveGlobal 刷新失败分支追加「保存已生效，但刷新视图失败」info 行〔不覆盖 refreshView 错误信息，双事实呈现〕、index.mjs/state.mjs 陈旧注释批〔advisor_config 枚举与 advisorOverride 结构注释补 runner〕）。**⚠️ 2026-09-11 反转标注（D-30，历史保留不删）**：本条修复所引入的**密钥链与 HMAC 签名腿随后被 D-30 整体删除**——威胁模型复核（需求档 §2.3：门禁刻意 fail-open 属纪律护栏 / 真正挡住伪造的是记录全等匹配 / 密钥与 token 镜像同目录 ⇒ 能写后者者几乎必然能读前者 / 净效果 = 多一条独立运维失效轴而防伪造增量 ≈ 0）判定该密钥链的价值为负，且上游 thincoder 已独立得出同判并删除签名段。D-25 当时要治的「公开默认值可伪造」随之**不再存在**（签名腿没了，不存在可伪造的签名）——现行授权链 = 两段式 `uuid:expiresAt` + **记录全等匹配** + **设计文档集指纹** + 流程纪律。本条的历史记录（含「TTL 1h 限制失效面」的论证）保留，但其结论已被 D-30 取代。 | lib/advisor.mjs | —（已闭环） |
| D-26 | 一致性 | eng.mjs 评审跟进批（2026-09-05 D-24 补跑发现，全部非阻塞）：①🟡 codex 分支 warnings 与 timeout/idle 本地回落告警；②🔵 engineeringToggle 移除未用 configDefaultEngineering；③🔵 单飞拒绝补 warnPrefix；④🔵 codex/dsh 空输出统一交付并显式「(empty report)」；⑤🔵 F10 盘回填区分已签发未传与从未签发；⑥⑦🟡 eng dsh 同步簿记改用模块级 deliverBookkeeping，四路单一实现；⑧🟡 dsh 后台兜底超时信封 code=ABORTED 触发回滚指引；⑨🔵 escalate codex jobs reject 清理 codexThreads；⑩🔵 eng 兜底 abort 文案空格句读统一 | 已定位 eng.mjs/escalate.mjs（R6 交付 2026-09-07） | 🟡(①⑦⑧)/🔵(其余) | R6 维护轮 | **已修**（2026-09-07 R6 终态：十项处方全部实施——实现 WIP `9318826`〔中断轮交付 + 架构师逐项审计通过〕+ 纯断言轮收口〔`test/codex-runner.test.mjs` 新增 4 test 块：①③④⑤⑥⑦⑧⑨⑩ 各 ≥1 断言；②编译级核销 = node --check 三文件通过 + 全量绿——纯无行为重构无断言面；④ 如实注记：eng codex ok+空文本经真实 adapter 不可达〔唯一 OK 出口要求 text 非空〕，按验收「两路径空输出断言一致」以 dsh 行为断言〔交付 + "(empty report)" + completed 簿记〕+ codex 分支源级一致性断言〔分类 env.ok 单条件、无 env.ok && env.text 残留、三处交付行共用 `(outputText \|\| "(empty report)")` 表达式〕交付〕；全量 node --test **233/233 绿**〔229 既有 + 4 新增〕；版本 0.9.1。**+ code review 微修轮**（2026-09-07：⑧ reject-race 分支补回滚指引〔与 resolve-race/escalate 对称〕+ 断言；② session-state.test 两处旧签名迁移 + 兼容 shim 删除〔干净收口〕；③ eng/escalate 后台派发句柄补 warnPrefix〔对齐 advisor R4 先例——配置告警即时可见〕+ 次序断言；**235/235 绿**） | lib/eng.mjs | —（已闭环） |
| D-27 | 执行架构 | dsh 路径仍受平台墙钟约束：advisor dsh 循环 900s 预算被 D-17 钳到 540s（**2026-09-06 生产复现**——用户撞到钳制告警「评审预算 900000ms 已按 budgetCapMs=540000ms 截断」）；escalate/eng dsh 子代理同步路径同理（DP-1 方案 A 设计边界）。根治 = 方案 B：三路径全部插件自建后台化，不改平台默认 | 已定位（D-17/DP-1 方案 A 边界；用户 2026-09-06 裁定升级为方案 B 全量 + 显式参数触发） | 🟡 | R5（设计 §7：advisor dsh 自动按 timeoutMs 判定；escalate/eng 显式 background 参数 + dshBackgroundTimeoutMs 兜底 30min） | **已修**（2026-09-06 R5 微轮 2/2：escalate/eng dsh 显式 `background` 后台 job〔run() 内启动子代理、dshBackgroundTimeoutMs 兜底、cancel→abort、成功簿记一次〕；jobs 缺失/派发抛错按两段式降级回同步+钳制告警；同步默认路径保持不变；工具 schema 与版本 0.9.0 收尾） | lib/eng.mjs | —（已闭环） |

| D-28 | 执行架构 | **会诊跨回合彻底失效**：consult 子代理继承调用方 `exec.signal`（PTC run-scoped 控制器，run_code 程序 settle 即 abort）→ 启动 1~3 秒内被 `child.cancel({kind:'parent'})` 杀掉，stopReason=aborted 且从未发出模型请求。**2026-09-08/09 lore 会话生产实证**：21 次尝试 20 次失败；唯一成功的一次是启动程序被一个 61 秒的 grep 卡住没结束 | 已定位（`lib/consult.mjs:111/:119` 信号继承 + `lib/index.mjs:928` 传 exec.signal；平台侧 `dsh-tools/lib/types/ptc.js:375` 注入 / `:532` 程序 settle 时 abort） | 🔴 | 2026-09-09 生产修复轮 | **已修**（控制器自持 + 看门狗超时归因 + 5 用例回归，见 CHANGELOG 0.9.2） | lib/consult.mjs | —（已闭环） |
| D-30 | 生命周期 | **design token 限期作废、只能靠再评审重铸**：会话跨度超过 TTL（缺省 1h）后已通过的设计令牌过期，而 token 只能由一次新的设计评审铸造 → 每次到期都要重跑一整轮设计评审（批 1/D-29 让 TTL 可配只缓解了频率，未解决机制）。附带三处结构性缺陷：① 签名腿与密钥链（D-25）带来启动全量作废 + 独立运维失效轴，防伪造增量 ≈ 0（门禁刻意 fail-open + 记录全等匹配才是真防线 + 密钥与 token 镜像同目录）；② `token-store` 保存路径只落三字段 → 传入的 `docHash` 被静默丢弃；③ 全局清扫「过期即删」→ 顺手删掉**别的会话**可续期的记录，让「重启后续期」非确定性地失败 | 已定位（`advisor.mjs` `TOKEN_TTL_DEFAULT_MS`/密钥链、`eng.mjs:372` 过期文案、`token-store.mjs:72-80`/`:84-89`；上游 thincoder 已独立删签名段） | 🟡 | 2026-09-11 D-30（批 2） | **已修**（设计档 `docs/2026-09-11-token-lifecycle-design.md` FR-T1…T9 / AC-1…AC-27 / 决策 D1…D11：token 两段化 `uuid:expiresAt` + 密钥链整体删除 + 审批码改 `sha256(uuid).slice(0,8)`（R2 防伪造语义保持）+ 缺省 TTL 7d（base 同步 604800000）+ **文档集指纹门控续期**（落点 = `eng_coder` 过期子分支；指纹 = 路径+内容双绑、快照取历轮并集、空集不绑、未过期不校验）+ 四条过期文案 + token-store 两缺口修复（补字段 + 清扫保留「过期但有指纹」记录 30d）+ 旧三段式令牌诚实识别 + 会诊跨回合口径修正；**验收面 = AC-1…AC-27 共 27 条中 26 条有专用用例 + AC-12 由 AC-13 的无 `docHash` 记录路径实质覆盖（交付代码评审原话确认）；全量 `node --test` 274/274 绿**）〔批 2 交付时点；批 3 落地后全量 **301/301** —— 历史值不改写〕 | lib/token-store.mjs | —（已闭环） |
| D-31 | 配置面 | **未知字段被静默丢弃，却返回成功——真实严重度 = 静默清空整个 user 层配置**（R-21 订正：原记「单字段未落盘」，低估了它）：`validateGlobalUserConfig` 把不在白名单的顶层键归入 `notes`（提示）而非 `errors`，`sanitized` 中不含该键 → 前端据 `errors` 为空报「已保存」；**全未知键**时 `sanitized === {}` 而 `ok` 仍为 `true` ⇒ `saveUserConfig({})` **整体替换**写盘 ⇒ **既有 user 层配置被整档清空**。父侧实测（2026-09-12）：`{bogusField:123}` → `ok=true, errors=[], sanitized={}`；而非法值 `{consultTimeoutMs:5}` → `ok=false`（走 errors）—— 故**只有「未知字段」这一支是静默的**。用户实测痛：设置页保存超时后「变回默认」（当时运行的插件尚无 D-29 的白名单，字段被判未知） | 已定位（`lib/index.mjs` `validateGlobalUserConfig`：`unknownTop` → `notes.push("ignoring unknown top-level field …")`；白名单 `topAllowed`） | 🟡 | 待开工（用户 2026-09-12 裁定：**单独成批**，不与批 3 混） | **已修**（批 10 · 2026-09-13：`unknownTop` 由 `notes` **改入 `errors`**（消息镜像嵌套分支的既有形态）⇒ `ok:false` ⇒ PUT 返回 **400** ⇒ **在写盘之前就拦下**——「静默清盘」路径灭绝。`saveUserConfig` 的整体替换语义**未动**（D10-12）。落点：`lib/index.mjs` 的 `unknownTop` 循环 + `test/config-api.test.mjs` 的 U3c 新契约 + 白名单一致性锁（D10-10）；独立回归锚 = 「全未知键 ⇒ 400 且盘字节不变」） | lib/index.mjs | —（已闭环） |
| D-32 | — | **号段保留**（未使用；2026-09-12 分配 D-33/D-34 时跳过，留作备用） | — | — | — | — | — | 永不（号段保留：编号占位，无机制可重看） |
| D-33 | 评审协议 | **评审员表达了通过但宿主未识别 → 工程流程静默卡死**：`isApprovalVerdict` 的通过词表只有三个词（`通过` / `批准` / `approved`），评审员的通过表述（「无未决 🔴、无新增阻塞项，可交 eng_coder 实施」）**命中数为 0** → `verdictPassed=false` → 不签发、不落盘；且失败**完全静默**（诊断被 `verdictPassed` 门住）。2026-09-12 现场复现于批 3 第二轮设计评审：评审 PASS 但 `eng_coder` 报「从未签发」 | 已定位（`lib/advisor.mjs` `APPROVAL_VERDICT_RE` 词表；`finalize` 的签发判定与诊断追加条件）；父侧实测 `isApprovalVerdict("I approve this design.")===false`、`("VERDICT: PASS")===false` —— **非中文特有**，任何非三词的通过表达都会中 | 🔴（阻断工程流程） | 2026-09-12 并入批 3（设计过程中现场复现） | **已修**（批 3：VERDICT 行成为主信号 + 六路可区分诊断 + 决策 D-h 明确**不扩词表**；验收 AC-V18 用实测评审文本做回归锁；全量 301/301） | lib/advisor.mjs | —（已闭环） |
| D-34 | 评审协议 | **撤销条件过宽：一次后续轮的 miss 会销毁先前已签发的有效令牌**：`if (completed)` 覆盖「每一个完成但未签发的 design 轮」——含 verdict 通过但回显缺失/不符，以及**成功签发之后的后续轮** → 令牌被静默清掉（`state.designToken = null` + `removeTokenRecord`），且 `removeTokenRecord` 的返回值被丢弃、无任何留痕。会诊 id 3 **独立提出**（父侧漏了这一条） | 已定位（`lib/advisor.mjs` 撤销分支；无「本轮是否曾签发」判别） | 🔴（销毁已获授权） | 2026-09-12 并入批 3 | **已修**（批 3 FR-5 / 决策 D-i：仅显式 FAIL 或「无 verdict + 回落判不通过」才撤销；回显问题**只诊断不撤销**；`removeTokenRecord` 返回值收集并 warn；验收 AC-V21 四个子case 全有真断言 + AC-V22） | lib/advisor.mjs | —（已闭环） |
| D-35 | 评审协议 | **设计评审链是「会话级」而非「文档集级」→ 同一会话里的第二份设计文档拿到的是收敛轮提示词，而非 round-1 设计提示词**：轮次键 = `sessionState.advisorRound`（会话级），只有 `reviewType` 变化才重置；`documents` 换成另一份设计文档时轮次继续递增 → 提示词选择按 `round >= 2` 走 `advisor-round2.md`（收敛轮：**「只做验证，不找新问题」**），且 prior 注入的是**另一份文档**的上一轮发现。**2026-09-13 现场复现**：批 4 设计评审用新文档集发起，返回的是「复验前一轮 7 项发现」的收敛轮报告（复核对象是**批 5** 的文档），并据此**铸造了**一枚新令牌——即「令牌声称覆盖的文档从未被真正评审」。对使用者是静默的（报告读起来像一次正常评审） | 已定位（`lib/advisor.mjs`：`state.advisorRound` 会话级递增 `:1717` 一带；`typeSwitched` 只比较 `reviewType` `:1582`；提示词选择在 `lib/advisor-msgs.mjs` 按 round 分支；`hasPrior` 只看 `advisorRound > 0 && lastAdvisorOutput`） | 🔴（评审覆盖面积造假 + 令牌与文档集脱钩） | **待排**（建议并入批 4 —— 三振护栏本身就是「按文档集键」的机制，两者同一把键） | **待修** —— 修法方向：把「同一链」的判据从会话级改为**文档集级**（键 = 归一化文档集，与批 4 的 `designDocKey` 同一实现），文档集变化 ⇒ 轮次与 prior 重置为 round 1 —— **已修**（2026-09-13 并入批 4：FR-G9 链作用域按**文档集键**——换集即**等价类型切换**（round=0 / prior=null / 代际+1 / 落盘），与 F11 合并为**单一重置谓词** `needReset` 且重置动作各只执行一次；`session-state.json` 新增 `lastDesignDocKey`（**摘要形态**——AC-20 禁 raw 路径入镜像，父侧裁定接受）；验收 AC-G10 + AC-G11a/b；v0.12.0） | lib/advisor.mjs | —（已闭环） |
| D-36 | 工程模式 | **主机重启后写门禁拒绝主代理的合法小修**：`designToken` 是**会话内存态**（`session-state.json` 刻意不持久化它——D-30 的「新字段不进 session-state」契约），重启后 `state.designToken = null`，而**写门禁只看内存态** → 主代理无法对已批准的设计做交付后小修，报「engineering mode is ON and no design token」。仅 `eng_coder` 有回填路径（`lib/eng.mjs` 用传入令牌与磁盘记录匹配后 `state.designToken = rec.token`）。**2026-09-13 现场**：DSH 两次非正常结束（内存耗尽）后，父侧要做交付代码评审的 🟡 小修被门禁拒绝，只能改派 eng_coder | 已定位（`lib/eng.mjs` `makeWriteGate` 只读内存态；`token-store` 的磁盘记录未被门禁消费） | 🟡（摩擦，**fail-closed 方向安全**：拒绝而非放行；代价 = 重启后必须过一次 `eng_coder` 才能恢复内存态） | 待排（与批 10 台账面同族；修法候选：门禁在内存态为空时按会话 id 查磁盘记录并回填——须先裁「这是否削弱门禁」） | **已修**（批 10 · 2026-09-13：`makeWriteGate` 在**内存态为空**时显式读盘（`loadTokenRecord`）→ **三态路由**（valid ⇒ 回填 `state.designToken` + 落一行日志 + 放行；expired ⇒ **复用**既有 expired 文案分支；missing ⇒ **复用**既有 no-token 文案分支）；**内存路径逐字不动**、**docHash 不进门禁判据**（D10-14）、**读盘失败 ⇒ 拒**（不落入 `:264-265` 的 fail-open catch，D10-15）；推翻既有裁定 D1 已设**专节**（设计档 §10）并**同步改写 `eng.mjs` 的该注释**） | lib/eng.mjs | —（已闭环） |
| D-37 | 测试纪律 | **「零改面」锚用「工作树 vs HEAD」形态 → 只在提交前成立，之后必然误红**：`test/design-review-guard.test.mjs` 的 **T-G9** 断言 `git diff --name-only HEAD -- lib/eng.mjs lib/prompts.mjs lib/prompts` 为空——它的**真实语义**是「批 4 的交付没有碰 eng.mjs/prompts」，但写法把它绑成了「**当前工作树相对 HEAD 没有任何未提交改动**」。于是①批 4 提交后该断言自动转绿（看似合理），②**其后任何一批只要改动 `lib/eng.mjs`，在它提交前的整个窗口里该断言必然红**。**2026-09-13 现场**：批 6 交付时全量 356/355/**1 fail**，唯一失败即此锚（批 6 设计档 §10.1 **明确要求**改 `lib/eng.mjs`）；实现者按纪律**没有**改既有测试，如实上报 | 已定位（`test/design-review-guard.test.mjs` T-G9 的 `git diff --name-only HEAD --` 形态；批 6 交付报告 §6-1） | 🟡（**误红噪声**：真实回归会被淹没在必然红里；且诱使实现者「顺手改测试」——正是本仓最忌的动作） | 批 6 修复轮 | **已修** —— **2026-09-13 父侧回盘核实**：采**修法方向①**，两处锚均已改造为**历史事实锚**并**同步修正注释**（注明断言的是历史还是工作树）：① `test/design-review-guard.test.mjs` **T-G9** 现取 `BATCH4_SHA = "2e6ca8b"`，对 `git show --name-only --format= <sha> -- lib/eng.mjs lib/prompts.mjs lib/prompts` 断言为空（锚实现在 `:767-776`，说明注释在 `:763-766`——「原锚是工作树卫生 ⇒ 批 6 合法改 eng.mjs 时误红」，并保留无 git 机器的跳过分支）；② `test/death-provenance.test.mjs` **T-AP9** 拆为「锚 A = 历史事实（引入提交自身不得改既有 test 档）+ 锚 B = 工作树 vs **固定基线** `2e6ca8b`」，并**显式标注断言的是历史还是工作树**（设计说明在 `:920-924`），`AP_TEST_AUTHORIZED` 的注释亦注明其来历（`:927`）。**登记表滞后说明**：本行「状态」列在本次核实前仍写「待修」，属**登记滞后**（批 6 修复轮已交付，见 CHANGELOG 0.13.0 与 test/ 内实证）——本次订正为**已修**。**比缺陷本身更值钱的纪律（仍有效）**：锚必须写清「**何时点**（历史提交 vs 当前工作树）」——批 6b 的 A1…A10 · 批 7 的 B1…B11 · 批 8 的 C1…C10 均按此写全；批 8 另追加同族一条：**在 `core.autocrlf=true` 的机器上，「零改动」不能只靠 `git diff` 证明**（git 在 diff 前归一 EOL，「只改行尾」会被吞掉），须另用字节级读取佐证。**原始记载（保留，含当时的修法方向）** —— 修法方向二选一：① 改成**历史事实**锚（`git show --name-only --format= <批 4 提交 sha>` 不含这两个路径）；② 删该子句，零改面由「源码字面锚 + 交付时人工回读」承担（批 4 §8.1 其余锚不变）。**两者都必须同步修正它的注释**（说清断言的是历史还是工作树）。**2026-09-13 同一教训连续命中两次（补记，提炼为规则）**：① `test/death-provenance.test.mjs` 的 **T-AP9 锚 B** 用 `git diff --name-only <基线> -- test`，把**本批新增**的测试档也算成「改动」⇒ **在提交那一刻自行转红**；② 加 `--diff-filter=MD` 仍不够——**锚所在的那个文件自己也被修改过**（就是这次修正）⇒ 它仍以 `M` 出现。最终语义 = 「**基线时点已存在**的测试档，除授权例外外零修改」：先 `git ls-tree -r --name-only <基线>` 取基线文件清单，再与「自基线起的 M/D 集合」**求交**。**提炼规则**：零改面锚必须同时写全三件事——**何时点**（历史 vs 工作树）、**何谓改**（新增 / 修改 / 删除要分开）、**自指**（锚所在文件自身是否在集合内）。三者缺一，锚会在某个时刻自行转红，而**假红会淹没真红**。 | test/design-review-guard.test.mjs | —（已闭环） |

## 已核清的非缺陷（审计确认，无需修复）

- **settings.yaml 剩余消费点**：文件读取只剩 profile-root 探测标记（dsh-home.mjs:37）；服务级读取（llm-pi-ai）为有意的分层富化源——数据源漂移问题整类清零（原 D9 收口）。
- ~~**consult 跨回合免疫**（N5）：startConsultChild fire-and-forget，回复在后续回合读取——jobs 迁移判据下无需迁移（需求 N-1 已固化此判据）。~~ **2026-09-09 撤回（D-28）**：该论断只在「子代理信号与调用方程序解耦」时才成立——原实现把 `exec.signal` 直传子代理，跨回合协议在生产上 100% 失效（21 次尝试 20 次死于 1~3 秒内）。「fire-and-forget 无需迁 jobs」的判据本身不变，但它的前提由 D-28 修复补齐。
- **advisor/codexCli 组字段三面白名单**：PUT = merge = 运行时解析，同步无漂移（漂移仅在 D-13 的 override runner 一处）。

## 平台契约要点（设计依据，审计 Task C 钉死）

1. `jobs.start(spec)` 返回 **branded string** `<kind>-N`（非对象）；每 owner 活跃上限 10；run() 内**无墙钟**（jobs 免疫 600s）。
2. 完成通知**只含一行指针**（bounded），永不含输出全文；全文经 `job_output` 读取，读取截断为**保尾**语义；原始输出在 registry 中不截断。
3. cancel → hooks.cancel → 插件 ctrl.abort → killTree；owner/session 优雅销毁会 cancel+await；**宿主硬死无任何 dispose**（B9 边界保持）。
4. `job_output wait:true` 平台侧上限 600s（与墙钟同值竞速）——等长 job 必须事件驱动接续。
5. maxWallMs 对已注册插件工具 execute() 的精确绑定路径未在平台源码中钉死（唯一实现于 dsh-code-runtime-worker-thread）；经验事实（本插件 D.1 实测 600s 截断 + 两个会话 15+ 次同形截断）作为设计依据，R2 加断言观测留证。

## 真机验证记录（2026-09-06，设计 §9 第 3 条收口）

| 验证项 | 结果 | 证据 |
|---|---|---|
| advisor 主路径 + token 签发（D-25 新 secret） | ✅ | glm-5.3-flash/off（最近档回落链）完成三轮完整评审并签发 token `3c2b9011…`（有效至次日 10:49） |
| eng_coder 全链 | ✅ | 新 secret token 校验 → dsh 后端执行 → 冒烟交付 1/1 pass → 脚手架即时清理 |
| consult | ✅（D-28 后须按跨回合形态重验） | 单模型（glm-5.3）会诊回复到达（内容诚实：拒绝编造时间戳）。**注**：该次冒烟成立的前提是启动程序活到了子代理完成——D-28 修复后「本回合 start、后续回合 check」才真正可用 |
| 受害场景② effort 秒死 | ✅ **活体治愈** | `zai:glm-5.3-flash + effort=low`（mal-analyze 会话三连败同款组合）→ 尾部出现 `[thincoder-suite] effort "low" is not supported … falling back to nearest supported effort "off"（D-裁决-2 最近支持档，等距向上取）` + 评审完整交付 |
| 受害场景① 空响应 | ✅ 按回退规则核销 | 自然复现不可求（需推理烧光 16384）；等效验收 = 215 套件空响应重试/分类/观测用例组（R1+R3 交付）；生产复现实录本表 D-01 留证 |
| escalate（含 followup） | ⏸ 结构性约束 + 单测覆盖 | 工程模式禁用 escalate（「实现归 eng_coder」纪律，拦截实测工作正常）；R2 迁移路径/deadline/单飞/followup 预算均有专项用例；真机冒烟可在非工程会话 30 秒补做 |
| D-15 idle 测量 | ✅ 完成 | 见 D-15 行：3 次 high-effort 真实任务，最大静默间隙 50.1s << 240s 阈值，维持 300s 默认 |
| D-13 runner 配置面 | ✅ 活体 | `advisor_config set round1.runner` 接受并生效（R4 前被拒） |
| 工程模式写锁 | ✅ 活体 | 基线期拦下 package.json 直改（热修走用户授权例外）；escalate 拦截工作正常 |
| **R5 终验（2026-09-07）：dsh >cap 自动后台化** | ✅ **活体** | dsh 路由（deepseek-official:deepseek-v4-pro/high）+ 900s 预算 → **自动派后台 job `advisor-dsh-1`，job 内预算不钳制、挂死兜底提示齐备、钳制告警零出现**——用户原始投诉场景（D-27 生产复现）结构性根治；background 全链在 R5 实施期间活体验证（codex 后端 eng_coder 派发即句柄→通知→交付，虽 30min 看门狗杀于报告阶段但实现完整交付 229/229——失败告警与回滚指引同时正确触发） |
