# 设计（批 29）：把门禁变可信 —— 残项清零（D-50 · D-51 · A28-6 机验腿 · 7 条 🔵）

- 日期：2026-09-26
- 状态：**设计待评审**（等用户发起第 1 轮设计评审）
- 来源：**全部为已登记项**，无临时加戏——**D-50**（🔴 流程：`eng_coder` 自跑进程内验证撞兜底，两次同款）· **D-51**（🟡 `T-AP4b` 偶发红，附实测频次）· **A28-6 的机验腿**（批 28 §6 明文划出的候选）· 批 27 的 3 条 🔵（交付码评 Deferred）· 批 28 的 4 条 🔵（交付码评 Deferred）
- 前序：[`dsh017-batch27-design.md`](./dsh017-batch27-design.md) · [`dsh017-batch28-design.md`](./dsh017-batch28-design.md)（§6 已列本批候选）

## §1 背景与用户故事

**背景**：24→28 五批把功能面收干（报告落盘 / 验证门归位 / 清扫 / 版本哨兵 / 巡检 / 结项）；剩下的问题**全部在「可信度」这一层**：兜底掐掉自己人的时间、偶发红让门禁读数不可信、锚面留了一处机验空缺、以及七条不致命但会累积的 🔵。

- **US-1（D-50）**：`eng_coder` **不能再自己跑验证**——现在只有一句**劝告**（任务书文字），实测两次没用：批 26 的 `eng-dsh-13` 自跑验证挂 8 分钟被掐、批 28 的 `eng-dsh-6` 自跑「进程内完整测试扫描」**满 60 分钟**被掐。要把它变成**机制**：让它**根本跑不了**，跑不了也不该白等。
- **US-2（D-51）**：`T-AP4b` 偶发红**先要能被诊断**（现在失败详情都留不下，全靠重跑撞运气）——把它变成**可复现、可定位**，再消除时序依赖。
- **US-3（A28-6）**：给复现脚本的只读性补**机验腿**（现在靠人眼核验背书），顺手订正脚本报告里那处降级分支文案。
- **US-4**：把批 27/28 交付码评留下的 **7 条 🔵** 一次收干（3 + 4，逐条可查）+ **D-52**（2026-09-26 取证插入：会诊只读白名单无对位锁）。

**交付拆分（用户 2026-09-26 裁定 A）**：本批**拆两单**——**单① = US-1**（只碰 `lib/eng.mjs` 与 `lib/index.mjs` 的 `check` 语义：`toolFilter` 禁执行 + 静默看门狗 + 语义改名；**面最小、且落地后后续单即受本机制保护**——这是拆单的全部理由）；**单② = US-2 + US-3 + US-4 + D-52**。两单各自走「实现 → 分歧审计 → 交付码评」，登记级联由架构师在每单落地后收口。

## §2 方案

### §2.1 D-50：三道机制（**能拦就拦，拦不住就掐，都不行就如实标注**）
① **工具面禁执行（首选拦截）**：派发 `eng_coder` 时利用既有的 **`toolFilter`**（平台 `subagents.start` 的已知允许键之一，见批 26 平台触点锁 #5）**排除「执行命令」类工具** ⇒ 子代理**在工具面就没有跑测试的能力**。**如实核验**：先读平台暴露的工具名清单定出「执行类」成员（命令/shell/spawn 类），取不到清单或过滤后**仍能执行** ⇒ 该腿如实标「未生效」，退回 ②+③，**不许假装拦住了**。
**平台契约事实（2026-09-26 取证，单①直接依据）**：`dsh-tools/lib/index.js` 的 `restrict(filter)` 文档原文 =「global-tool mask: **`allow` (keep only)** and/or `deny` (remove)」；`dsh-subagent` 在起子代理时对子上下文调 `childCtx.tools.restrict(composition.toolFilter)`；`dsh-subagent/lib/types/descriptor.js` 里 `TOOL_FILTER_KEYS = new Set(['allow','deny'])` ⇒ **`deny` 是「移除」语义**（本单①用它把执行类工具从 eng_coder 的工具面移除）；白名单/黑名单里出现**未注册的工具名会被响亮拒绝**（不是静默忽略）⇒ 工具名必须取自平台实际注册面，取不到就按 A29-2 **如实标注未生效**。

② **静默看门狗（兜底省时）——住新叶子模块 `lib/silence-watchdog.mjs`（D29-6，评审后订正）**：轮询定时器与 abort 写点**一律不进 `lib/eng.mjs`**——T-AP7 把四机制文件的**定时器时长字面**与 **`.abort(` 写点数**逐字冻结（并与 HEAD 交叉核验），批 26 已证过「`eng.mjs` 里再加定时器/写点 ⇒ 结构上不可满足」；当时把宿主验收执行面抽成 `lib/host-check.mjs` 才解决（D48-5），本单复刻同一路径。

**已知锁交互清单（由单①半成品实测总结，逐条采信）**：① T-AP7(c) 冻结各文件**既有** `setTimeout` 第二实参表达式 ⇒ 看门狗轮询用 `setInterval`（`eng.mjs` 仍只有 `backstopMs`/`budgetCap` 两个 setTimeout，一字未动）；② T-AP7(b) 冻结 `.abort(` 写点数 ⇒ **不得**为看门狗新增 abort 写点（这正是半成品失败处：它把 abort 抽成共享闭包后写点数变了）⇒ 看门狗模块**自行持有**中止手段（导出 `shouldAbort`/`onSilence` 供 `eng.mjs` 调用，由**既有**写点执行 abort）；③ `failStop` 计数锁恒 **18**（doc-hygiene V6 / advisor-config A7）⇒ 静默终止信封**沿用** `FAILURE_OPTIONS_BLOCK` 常量，不新造 wrapper；④ `stages.test.mjs` 有 T12「干净路径返回文本零 warning 前缀」⇒ 机制未生效类标注走**裸 `console.warn`**（不进 `warnPrefix` 返回文本）；⑤ `stages.test.mjs:245/265` 的集成腿断言 staged 块渲染文本 ⇒ `check` 改名会改它 ⇒ **本批明文授权同批更新**（见 §3 授权例外）。

② **静默看门狗（兜底省时）**：**心跳来源钉死（评审 #1）**——**不得**用 `job_output` 轮询（该读取面在本机实测是坏的，正是 D-45 立项前提）；心跳取自**作业 `run()` 内对子代理运行事件的观察**（子代理在 D-27 定的启动点起动，其输出与工具调用事件即心跳；`handle.append/updateProgress` 亦然）。**取不到事件流 ⇒ 退回总预算并如实标注「静默看门狗未生效」**（不许假装在看）。派发期记「最后一次心跳时刻」；**静默超过 `ENG_SILENCE_ABORT_MS`（默认 5 分钟）即 abort**，终止文案记「**疑似挂死（静默 N 分钟）**」并附已产生的 partial ——与现行 `dshBackgroundTimeoutMs`（60 分钟）是**两层**：静默看门狗管「卡住」，总预算管「长任务」。
③ **任务书措辞订正**：`stages[].check` 的语义从「子代理的自检命令」正式改为「**宿主验收清单**」（`checkMode: host` 时渲染成「由宿主执行，你**不要**执行」；不得再出现「由我确认」这类暗示它自行验证的措辞）。

### §2.2 D-51：先可诊断，再消时序
**范围与复现次数钉死（评审 #3）**：目标档 = `test/death-provenance.test.mjs`（`T-AP4b` 所在档）**逐腿**，同类时序腿的判据 = 该档内**依赖真实等待/并发时序**者（实现时逐条点名并写进交付报告）；受控负载复现 **N = 20 次**。① **失败可诊断**：给该腿（及点名到的同类腿）加**失败差量输出**——断言失败时把 `actual/expected` 与关键状态摘要写进测试输出（不依赖重跑撞见）。② **定位**：用 `--test-reporter=spec`（或等价）在**受控负载**下复现（全量并发跑，重复 N 次）。③ **修**：若确认是真实等待/并发敏感 ⇒ 改**可注入时钟/夹具**（禁止真实 sleep）；若确认是平台并发导致的资源竞争、且本仓无法消除 ⇒ 在门禁里**显式登记已知偶发**（重试一次 + §7 留痕），**不得默认允许**。

### §2.3 A28-6 机验腿 + 脚本文案（US-3）
新增机验腿（静态）断言 `scripts/repro-platform-issues.ps1`：除 `$env:TEMP` 下的临时目录创建/清理外**零写 cmdlet**、零 `git` 调用、零 `$env:X=` 赋值、纯 ASCII；并把报告里「its only write was a temp dir…」一句改为按 `$tempHomeOk` **分支渲染**（降级分支：`no temp dir was created this run`）。

### §2.4 七条 🔵 收口（US-4，逐条可查）
**来自批 27 码评**：① `sweepJobReports` 的 `maxAgeDays: 0` ⇒ 全删语义：**校验收紧为 `> 0`**（缺省 7 不变）并在 JSDoc 写明 ② `statSync` 失败 `continue` 会让摘要 `kept` 少计 ⇒ 注释注明「stat 失败不计入 kept」③ A27-4 的「只 warn 一次」依赖用例顺序 ⇒ 增加测试缝 `__resetWarnedNoHomeForTest`：**可见性机制（评审 #8）**——本仓无私有导出，故以**命名约定 + JSDoc `@internal`** 为准，并加**静态锁**断言「生产路径（`lib/**` 非该档自身）零引用该缝」；测试档内使用时须在用例头部注明「仅测试」。**来自批 28 码评**：④ 源②的键过滤谓词化简为 `k.startsWith("@deepseek-ai/dsh")` ⑤ 哨兵 `once` 标记补注释「`unknown` 与 `mismatch` 共用 ⇒ 首个 unknown 会占坑」⑥ 版本词元比较**统一小写**（`toLowerCase()`）后再比 ⑦ 脚本文案（见 §2.3，同一处）。

## §3 受影响文件

| 文件 | 改动 |
|---|---|
| `lib/eng.mjs` | 派发时 `toolFilter` 排除执行类工具（含「取不到清单 ⇒ 如实标注未生效」分支）· 静默看门狗（`ENG_SILENCE_ABORT_MS` 默认 300000）· 任务书 `check` 措辞改为「宿主验收清单」 |
| `lib/job-outcome.mjs` | 🔵①②：`maxAgeDays > 0` 校验 + JSDoc 写明全删语义 · `statSync` 失败注释 · 测试缝 `__resetWarnedNoHomeForTest` |
| `lib/contract-baseline.mjs` | 🔵④⑤⑥：过滤化简 · `once` 注释 · 版本词元 `toLowerCase()` |
| `scripts/repro-platform-issues.ps1` | 🔵⑦：报告文案按 `$tempHomeOk` 分支 |
| `test/silence-watchdog.test.mjs` | **新增**：静默看门狗（到点 abort + 有输出续命 + 文案含「疑似挂死」）与 `toolFilter` 生效/未生效两态 |
| `test/contract-baseline.test.mjs` | 🔵④⑤⑥ 的对位断言（大小写等价、once 注释在场、过滤结果集） |
| `test/job-persistence.test.mjs` | 🔵①②③ 的对位断言（`maxAgeDays:0` 拒绝、`kept` 注释口径、测试缝可用性） |
| `test/repro-script-readonly.test.mjs` | **新增**：A28-6 机验腿（脚本静态只读断言 + 文案分支断言） |
| `test/death-provenance.test.mjs` | D-51：失败差量输出 + 消除时序依赖（若确属平台并发 ⇒ 按 §2.2③ 登记并留痕） |
| **新增** `lib/silence-watchdog.mjs` | **D29-6**：静默轮询器（`ENG_SILENCE_ABORT_MS` 缺省 300000 · `ENG_SILENCE_POLL_MS` 1000 · 注入缝）；`eng.mjs` 只 import——**零新增定时器字面、零新增 abort 写点** |
| `test/stages.test.mjs` | **仅**：`check` 语义改名导致的**渲染文本**对位更新（`:245/265` 集成腿）——列入 **D-37/T-AP9 授权例外**（理由：D29-3 改了 hosted 渲染文案） |
| `test/guard-e.test.mjs` | **仅** T-E19 `existing` 清单登记本批新增两档（沿用批 27 分工裁定） |
| `lib/consult.mjs`（**单②**） | D-52：不改行为，只保证「白名单成员集可被静态断言、`toolFilter` 可被行为断言」；若需可测缝则最小化并标 `@internal` |
| **基线锁授权例外（评审 #5）** | **D-37 / T-AP9 的「基线时点已存在的测试档零修改」锁**：本批明文**授权修改** `test/death-provenance.test.mjs` · `test/job-persistence.test.mjs` · `test/contract-baseline.test.mjs` · `test/guard-e.test.mjs` · **`test/stages.test.mjs`**（单①：`check` 改名后的渲染文本对位）五档（各有本批理由，逐条见上表）；若该锁有**文内例外清单** ⇒ **同批登记**并写明理由，机制上无清单则**在交付报告与 §7 留痕**——**不留给被自家锁打红再临场解释**（先例：批 28 的 D28-6） |

**分工裁定（沿用批 27 §3）**：`docs/**`（文档地图/台账/CHANGELOG/版本/登记表/本档）由**架构师**在实现落地后收口；eng_coder 白名单 = 代码 + 测试档（外加 T-E19 清单行）。

## §4 决策

| # | 决策 | 理由 |
|---|---|---|
| D29-1 | **先拦后掐**：工具面禁执行优先，静默看门狗兜底，两者都不行则**如实标注未生效** | 与 D26-2/D27-1/D28-1 同一条纪律：不得靠「劝告」，也不得假装拦住了 |
| D29-2 | 静默阈值 `ENG_SILENCE_ABORT_MS` 默认 **300000（5 分钟）** 且可配 | 实测两次卡住的静默段都远超 5 分钟（8 分钟 / 60 分钟），5 分钟足以判「卡住」而不误杀长任务 |
| D29-3 | `stages[].check` 语义**正式改名**为「宿主验收清单」 | 语义错位是这两次事故的**根因之一**（字段名在暗示它自己跑） |
| D29-4 | D-51 **先可诊断再修**，禁止用重试掩盖 | 偶发红的可诊断性 = 门禁可信度的前提 |
| D29-5 | 七条 🔵 **一次收干**（不拆批） | 全是小面积改动，拆批只增评审轮次 |
| D29-6 | 看门狗**住新叶子模块** `lib/silence-watchdog.mjs`，`eng.mjs` 只 import | 批 26 已证：`eng.mjs` 再加定时器/写点 ⇒ 撞 T-AP7 **结构上不可满足**（D48-5 同款处置）。**本单首轮实现把它写进 `eng.mjs` ⇒ 实测撞 T-AP7 + `stages.test` 集成腿，判为设计缺陷（归架构师）并如实留痕** |

## §5 锚与判据

### §5.1 机验锚
| 锚 | 检索目标 | 谓词 | 期望 |
|---|---|---|---|
| **A29-1** | `test/silence-watchdog.test.mjs` | 派发期 `toolFilter` 生效 | 执行类工具**不在**子代理可用工具集内；非执行类（读写/编辑/检索）**仍在**。**证据口径（评审 #2，比照 A28-4 的 `evidence`）**：优先取**平台回显的生效工具清单**为证；平台不回显 ⇒ 以**派发时接受的 `toolFilter` 载荷**为证并**如实标注为弱证**（两种形态都必须在断言里区分，不得混同） |
| **A29-2** | 同上 | 取不到工具名清单的形态 | **如实标注「未生效」**并退回 ②+③，**不**报成拦住了 |
| **A29-3** | 同上 | 注入静默（无输出） | 到 `ENG_SILENCE_ABORT_MS` 即 abort，终止文案含「**疑似挂死**」与静默时长；**有输出则续命**直到总预算 |
| **A29-4a** | `test/death-provenance.test.mjs` | 注入一个必然失败的形态 | 输出含 `actual/expected` 差量（**不靠重跑**） |
| **A29-4b** | 同上 | 受控负载下连跑 **N=20** 次 | **零红**（不再依赖真实等待）；**或**走 §2.2③ 的显式登记分支时，期望改为「**登记条目在场 + §7 留痕**」（评审 #4：与该分支同口径，不再自相矛盾） |
| **A29-5** | `test/repro-script-readonly.test.mjs` | 静态扫 `scripts/repro-platform-issues.ps1` | 除 `$env:TEMP` 临时目录外**零写 cmdlet**、零 `git`、零 `$env:X=`、纯 ASCII；报告文案按 `$tempHomeOk` 分支 |
| **A29-7**（**单②**） | 既有 consult 测试档（或新增 `test/consult-readonly.test.mjs`，须登记） | D-52 对位 | **静态**：`lib/consult.mjs` 的白名单成员集恰为只读集合且 `allow` 键在场（不得出现写类工具）**行为**：`subagents.start` 收到的 spec 里 `toolFilter.allow` 逐项在场、且**不得**出现 `deny` |
| **A29-6** | `test/job-persistence.test.mjs` · `test/contract-baseline.test.mjs` | 七条 🔵 对位 | `maxAgeDays:0` 被拒（缺省 7 不变）· `kept` 口径注释在场 · 测试缝可用 · 过滤化简后行为不变 · 版本词元大小写等价 · once 注释在场 |

### §5.2 验收标准
| # | 验收标准 | 层 | 锚 | US |
|---|---|---|---|---|
| **AC-1** | `eng_coder` 在工具面**没有**执行命令的能力（或如实标注未生效） | T1 | A29-1 · A29-2 | US-1 |
| **AC-2** | 静默到点即 abort 并如实记「疑似挂死」；有输出不误杀 | T1 | A29-3 | US-1 |
| **AC-3** | `check` 字段语义 = 宿主验收清单（任务书不再暗示子代理自验） | T2 | —（文本锁） | US-1 |
| **AC-4** | `T-AP4b` 可诊断（**A29-4a**）且不再偶发（**A29-4b**：N=20 零红）；**或**已显式登记为已知偶发并留痕（此时 A29-4b 按登记分支期望） | T1 | A29-4a · A29-4b | US-2 |
| **AC-5** | 脚本只读性有**机验腿**，文案按降级分支渲染 | T1 | A29-5 | US-3 |
| **AC-6** | 七条 🔵 全部收口且各有对位断言 | T1/T2 | A29-6（①–⑥）· **A29-5**（⑦ 脚本文案） | US-4 |
| **AC-7** | 登记级联：**D-50 / D-51 转「已修」**⇒ 待修 0 · 余项 0；台账/CHANGELOG/版本/事实 id 同步 | T3 | — | 全部 |

## §6 边界（本批不做）

- **不改平台**：`job_output` 读法缺陷只在上游报告包里（批 28 已交付），本批不碰。
- **不动发布物**：除本批清单外的 `lib/**` 与既有测试档不改。

## §7 变更记录
| 日期 | 变更 |
|---|---|
| 2026-09-26 | **单①第二轮失败（环境侧）+ 程序性例外：改道 harness `subagent`（用户裁定 B）**：`eng-dsh-24` **0 分钟 `error`、零产出、零改动**；同期证据——**14:22:32 两个作业同一秒死亡**（我的 `eng-dsh-22`〔33 分钟·partial 空〕与**另一会话的 `eng-dsh-23`**〔中途死、partial 有半句〕），而 12:25–13:48 同路径作业全部正常产出 5–9KB 报告 ⇒ **恶化点约 14:20，属插件 spawned-child 路径的环境侧故障**（判别探针：harness 自带 `subagent` 同刻跑通回 `OK` ⇒ 平台子代理面未坏）。用户因多会话在跑不能重启，裁定**改道**。**程序性例外（如实记录，不假装合规）**：本单第二轮的实现由 **harness `subagent`** 执行 ⇒ **旁路 eng_coder 的令牌/阶段门**；**补偿**：授权面与「五条锁约束」逐条写进子代理任务书，验收**加强**为「宿主全量 + 分歧审计 + 交付码评」三道，且本轮**不消耗**设计令牌（令牌留待环境恢复后复用或重铸）。 |
| 2026-09-26 | **单①首轮失败 + 设计订正（如实留痕，不粉饰）**：首轮 `eng-dsh-22` 跑了 32.7 分钟以 `error` 结束、partial 为空；工作区留半成品（`lib/eng.mjs` +288/-10，语法完好、`lib/index.mjs` 未动、新测试档未建）⇒ 全量 **565/2 红**：**T-AP7**（它把 abort 抽成共享闭包后 `.abort(` 写点数变化，违反「写点数不增」）与 **`stages.test.mjs:245/265` 集成腿**（`check` 改名改了渲染文本，而**首版设计没把该档列进受影响面**）。**根因归架构师**：看门狗放错模块（复刻批 26 的 T-AP7 坑）+ 未列渲染面受影响档。**订正**：① 看门狗改住叶子模块（**D29-6**）② 授权例外扩到五档（含 `test/stages.test.mjs`）③ 采信半成品总结的**五条锁交互清单**（见 §2.1）④ `lib/eng.mjs` 回滚到 HEAD 后重派。 |
| 2026-09-26 | **拆单裁定 + D-52 并入（用户裁定 A）**：本批拆 **单①（US-1：禁执行 + 静默看门狗 + `check` 语义改名）** 与 **单②（US-2 + US-3 + US-4 + D-52）**；D-52（会诊只读白名单无对位锁，2026-09-26 取证）并入单②，新增 **A29-7** 对位锁；§2.1 补**平台契约事实**（`allow` = keep only / `deny` = remove；未注册名响亮拒绝）作为单①的实现依据 |
| 2026-09-26 | **第 2 轮设计评审处置并入**（PASS；上表 1–5 经逐条核实 Fixed）：订正 ⑥ 登记表表头分解（号段保留 **2** = D-32 · D-43）⑦ AC-6 锚集补 **A29-5**（⑦ 的对位腿）⑧ 测试缝**可见性机制**钉死（命名 + `@internal` + 静态锁「生产零引用」）| 
| 2026-09-26 | **第 1 轮设计评审处置并入**（PASS，5🟡）：① 心跳来源钉死为「`run()` 内子代理事件观察」（**不用** `job_output` 轮询；取不到 ⇒ 如实标注未生效）② A29-1 补**证据口径**（平台回显清单为强证 / `toolFilter` 载荷为弱证）③ D-51 范围与复现次数钉死（目标档 `death-provenance` + 同类时序腿逐条点名；**N=20**）④ **A29-4 拆 a/b**（差量输出腿 + 稳定性腿），AC-4 与 §2.2③ 的登记分支同口径⑤ §3 补 **D-37/T-AP9 基线锁的授权例外**行 |
| 2026-09-26 | 首版：D-50 三道机制（工具面禁执行 / 静默看门狗 / 语义改名）· D-51 先可诊断再消时序 · A28-6 机验腿 + 文案 · 七条 🔵 收干；决策 D29-1…5 · 锚 A29-1…6 · AC-1…7 |
