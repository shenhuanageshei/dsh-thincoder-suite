# 交接页：thincoder 吸收批次（截至 2026-09-13 夜）

> **用途 = 新会话的入口。** 本页自足：读完它 + 它点名的几份档即可接续，**不需要旧会话的上下文**。
> **维护纪律**：每批收口后更新 §1（进度）与 §5（待办）。
> 单一事实源仍是 [`2026-09-12-absorption-inventory.md`](./2026-09-12-absorption-inventory.md) §6（批次排序）；本页只做「此刻在哪一步」的快照。

---

## §1 进度快照

| 批次 | 内容 | 状态 | 版本 / 提交 | 全量测试 |
|---|---|---|---|---|
| 批 1 | D-29 配置面（`consultTimeoutMs`/`engTokenTtlMs` 进 user 层白名单） | ✅ | `9ae1277` | — |
| 批 2 | D-30 design token 生命周期（两段化 / 删密钥链 / 指纹门控续期 / TTL 7d） | ✅ | `707706f` / v0.10.0 | 274 |
| 批 3 | 评审协议增强（VERDICT 收尾行 / 四值词表 / R1–R7e / 失败可见 / FR-6 撤销收紧） | ✅ | `b915344` + `d9fc805` | 304 |
| 批 5 | 上下文预算跟随模型窗口 + 估算器 CJK 加权 | ✅ | `c1966f8` / v0.11.0 | 320 |
| 批 4 | 设计评审豁免 5 轮上限 + 三振结算护栏 + **D-35 折入**（链按文档集作用域） | ✅ | `2e6ca8b` / v0.12.0 | 342 || 批 6 | 死亡可诊断（守卫 D 判定换血 + 四家族 abort 溯源 + 裸 abort 灭绝） | ✅ **完全收口**（交付代码评审 PASS：🔴0 · 🟡1 经裁定并入 R-1 · 🔵4 已处置） | `a5f9551` + `aa9f257` + `9205bc8` / v0.13.0 | 360 |
| **批 6b** | 守卫 E：在途窗口冻结 + 预闸拦截 + **结算侧指纹兜底** | ✅ **完全收口**（设计评审 轮次1 FAIL → 轮次2 PASS · 分歧审计 🔴0 🟡1 🔵5 → 修复轮 · 交付代码评审 **PASS** 🔴0 🟡1 🔵4） | `d1914aa` / v0.14.0 | 384 |
| 批 7 | 可移植性三件（METHODOLOGY 退役 / 提示词去本仓指涉 / 判据单一权威） | ✅ **完全收口**（设计评审 轮次1 FAIL → 轮次2 PASS · 分歧审计 🔴1 🟡2 🔵2 → 修复轮 · 交付代码评审 **PASS** 🔵2 → 收尾修复轮） | v0.15.0 | 399 |
| 批 8 | 提示词公共层（裁减版）——**只此一件**（D1–D7 归批 11，见吸收清单 §6 订正行） | ✅ **完全收口**（设计评审 轮次1 FAIL → 轮次2 PASS · 分歧审计 🔴0 🟡2 🔵2 → 修复轮） | `a3aaea9` / v0.16.0 | 413 |
| 批 9 | 测试生命周期三层 + 发布门 + `verify` 宿主侧门禁（**三项全做**——用户裁定形态全做 · R-13 不根治 · tag 带 `v`） | ✅ **完全收口**（设计评审 轮次1 FAIL 🔴2 → 轮次2 PASS · 分歧审计 🔴1 🔵5 → 修复轮 · 交付代码评审 轮次1 FAIL 🔴1 → 轮次2 PASS 🔴0 · 收尾修复轮） | v0.17.0 | 434 |
| 批 10 | 台账纪律（可落地版：计数有据 + 指针/触发字段）+ **D-31**（未知配置字段静默丢弃）+ **D-36**（重启后 token 内存态空）+ **文档卫生**（十二处滞后状态行） | ✅ **完全收口**（设计评审 轮次 1 `PASS` 🔴0 → 实施 8 stages · 23 次变异 → **独立分歧审计 🔴0 🟡3 🔵7** → 两轮修复轮 → 交付代码评审轮次 1 `PASS` 🔴0 · 🟡0 · 🔵5 → 收尾修复轮；发布门对本 tag 实跑 **6/6 机械闸通过**） | `0c15c69` / v0.18.0 | 449 |
| 批 11 | **D1–D7 文档纪律**（规范层进 `METHODOLOGY.md` 新子节、**现状标定只进设计档 §10**）+ 上游**失败路径语义**（只取「停产报告含五项恢复菜单 + 不自动重派」，落 `lib/eng.mjs` 返回串 + `lib/index.mjs` 的 eng_coder 描述**同批划界**）+ **DOC-HYGIENE 提示词级纪律**（`lib/prompts/discipline.md` 五条）+ **机械网新档** `test/doc-hygiene.test.mjs` | ✅ **已收口**（设计评审**五轮** FAIL×4→**PASS** → 实施 **9 stages** → **独立分歧审计 🔴0 🟡3 🔵5** → **交付代码评审 `VERDICT: PASS`**）。**★ 分歧审计 🟡3 订正**：本行初写「**8 stages · 19 次变异自证**」——**两个数都不实**：实际 **9 stages**；**变异记录只有父侧自行施加的 6 个**（实现者**零交付报告**，见设计档 §14.6.2/§14.6.4）。**无出处的具体过程断言，正是本批要治的物种** | `20fd3b4` / v0.19.0（**已推远端**；tag 已推） | 453 |
| 批 12 | **`WRITE_GATE_FIXTURE` 锁面收窄**（把「逐字节锁 `makeWriteGate` 函数体**含注释**、79 行 / 4821 字符」收窄为「**只锁可执行行**」47 行 / 2677 字符）+ **陈旧指针订正**（含一处**结论错误**） | ✅ **已收口**（设计评审**轮次 1 即 `PASS`** 🔴0 → 实施 **17 变异** → **独立分歧审计 🔴1 🟡3 🔵4** → **设计修订** → 修复轮 **25 变异** → 交付代码复评 **`VERDICT: PASS`**；两轮共 **13 条**发现**全部处置**）。**★ 审计抓到一个本批引入的真漏洞**：`stripComments` 只认 LF 而 JS 的 LineTerminator 有四个 ⇒ `//` 后插 CR/U+2028/U+2029 会让代码**在锁眼里消失、门禁实际 fail-open 而全绿** ⇒ 修法 = **先统一行终止符再剥离**（父侧先验后改）。**而设计档原写着「只可能误红、不可能假绿」——被我自己的推理写下的、被一个具体变异证伪**。**元教训**：对「某弱点不可能造成 X」的断言，本身就是最需要对抗性测试的一句 | `0f62c27` / v0.20.0 | 453 |

| 批 13 | **登记面与判据面余项**（§4 的 **R-9 · R-8 · R-6 · R-5 · R-4 · O-E5 · R-25**）+ **D-38 清偿** | ✅ **已收口**（设计评审轮次 1 `PASS` 🔴0 → 实施九 stage → **独立分歧审计 🔴1 🟡4 🔵5** → 设计修订 **D13-19/D13-20** → 修复轮 → **交付代码复评轮次 1 = `VERDICT: FAIL`**（🔴1）→ **父侧同步四处副本** → **轮次 2 = `PASS`**）。**★ 本批的主题是「三条登记描述是错的」**：**O-E5 说反了**（真实是**基分叉下的错根锚定**，**冻结侧 fail-open = 保护性损伤**）· **R-9 实测缺 4 项 / 2 文件**（非 1 键 1 文件）· **R-4a 是 3 处**（非 1 处）——**外加父侧简报自己也有一条错的**（「6 维句 design 与 code 都发」，**四家模型全部独立指出**）⇒ **「登记文本订正」被列为与代码修订同级的交付物**。**★ 本批最重的一次自纠**：R-4a 初版只看 `status === 128` ⇒ 会把「该 path 在该 revision 不存在」（**真回归**）降级成静默 warn（**比原问题更糟**）⇒ **收紧为同时匹配 stderr**（**只有 T-AP7 的 `HEAD:<path>` 语法暴露于该路径**——审计比父侧更准）。**★ 元教训**：**「枚举/规格不追列表」这个病，四层评审各抓住本批一次**（设计评审 #1 的 AC-22 · 审计 F3 的「家族四面自动扩」· 代码复评 #1 的**四处互斥副本** · 复评 #2 的**第五处**在代码块里） | `4f6e328` / v0.21.0 | 453 |
| **D-38** | 见 [`2026-09-05-defect-registry.md`](./2026-09-05-defect-registry.md)（按本页引用约定**只留 ID，不复制标题/状态/指针/触发**） | **已随批 13 清偿完毕**（**触发条件成立**：批 13 触碰 `lib/eng.mjs`；**且实测修它无需重基线夹具**——两处都在字节锁区间之外，**原冻结注未说明这一点**）。**登记表已同步为「已修」**，本表条数随之 36→37 时已同改 | — | — |**当前工作树**：**干净**（`viz/` 未跟踪，**故意不提交**）。**批 11 与批 12 均已完全收口并推送**（`a0eb6f3` / v0.19.0 · `0f62c27` / v0.20.0，tag 均已推）。**批 13 卡在「等用户发起设计评审」**（`273bad9` 已推，三档齐备）。

| **批 14** | **配置面与描述面同步**（**六项**，见 [`2026-09-15-config-surface-design.md`](./2026-09-15-config-surface-design.md)） | ✅ **已交付（v0.23.0）**——**但交付方式有异常，见下方 ★**。**六项**：① `engCoderEffort` 默认值 `low`→**`medium`**，且**回落目标与初值同源一个常量**（旧形态靠初始化值隐式回落）· ② 白名单散文**常设谓词**（三轴 **TOP 8 · ADV_SUB 6 · GROUPS 2 · GROUP_FORM 5 = 21**；逐文件各一条 + 先钉基数 + 双向）· ③ F8 遗留（**两分支都治**：ENOENT 打新写的准确话、128 维持既有的准确的、`:992` 的 else 只在两分支都未命中时触发）· ④ V7 的 `topAllowed` 面 · ⑤ 到点文案第一字面补 `codexCli.` 前缀 + **前置告警仅 dsh 同步路径** · ⑥ **`lib/**` 十处 / 七档悬空引用删指针**（`DESIGN-dsh-port.md` 7 + `DESIGN-advisor-token-protocol-fix.md` 3）。**★ 交付异常（如实登记）**：**`eng_coder` 在 `dshBackgroundTimeoutMs=1800s` 兜底被硬中断、未留下任何报告**（无 stage 表 / 无自证 / 无自白）；工作区留下看起来完整的实现（16 档、456/456 绿）⇒ **父侧未重派实施，改为「审计即验证」**：派独立分歧审计**代产实施者没写的自证**（**锚 A1–A12 12/12 可跑且通过 · 变异 5/5 真红 + byte-exact 还原 · 八项零改面 6 项干净 · AC 21/25 达成**）⇒ 审计另抓 **9 条**（其中 3 条 🔴 是**父侧设计措辞问题**、1 条是漏写「空操作」）⇒ 全部折入 ⇒ 再派一轮**小修复轮**补**唯一一条真代码缺口**（**「空集 ⇒ 必红」此前只是声明、没有腿证**；补法 = 把内联三次的护栏抽成 `assertDocKeysNonEmpty` + 两条互补变异）。**⇒ 流程教训**：**「实施 + 自证」压在一个 1800s 窗口里是设计错配** ⇒ **今后拆 stage 应把自证单独留给审计轮**。 | **v0.23.0**（`b065ed3`） | **456** |
| **批 19** | **effort 回落的开关语义**（`nearestEffort` 根修）：`off` 是**关闭开关**不是力度档 ⇒ **退出距离竞争**；回落只在力度域内进行；**退化兜底**（插件梯子上除 `off` 外无候选）才用 `off` 且带专属告警；**显式请求 `off` 而模型关不掉 ⇒ 省略 `reasoningEffort`（交还提供方默认）并明说**。见 [`2026-09-17-effort-off-fallback-design.md`](./2026-09-17-effort-off-fallback-design.md) | ✅ **已发布**（设计评审轮 1 `PASS` 🔴0 · 10 条 advisory 全 Fixed；**交付代码评审轮 1 `PASS`** 🔴0 · 🟡2 · 🔵1，3 条全 Fixed；**分歧审计子代理被中断 ⇒ 父侧自审替代**，见该档 §12.4） | `1b84609` / v0.27.0 | 485 |
| **批 20** | **`writeFileAtomic` 硬化 + 并入已知 flake `R-13`**（原子写：`rename` 有界重试 + 失败可见） | 🟡 **立项 / 摸底 / 需求档已落盘推送**（`f18b78d` 摸底 · `b27e1de` 需求）——**会诊 `#19` 在飞**；余下链路（纪要裁定层 → 需求定稿 → 设计档 → 设计评审 → token → `eng_coder` → 核验 → 分歧审计 → 交付代码评审 → 收口 `v0.28.0`）**待其返回后续做**。**★ 摸底档已答本批第一道门**：`R-13` 与 `R-51` **同源**（「时序 flake」是误诊——失败原文恰是「写落地前」的文件内容）⇒ **一次修两个** | — | — |

> **★ 五件挂起的事 —— 重启已发生（用户 2026-09-16 执行），实况如下**：
>
> | # | 内容 | 状态 |
> |---|---|---|
> | ① | `advisor.standardsDoc` / `advisor.documentMapDoc` **已落盘但未生效**（配置需重启） | **应已生效**（配置随重启加载）——**父侧无注入面，静默面只能由用户确认** |
> | ② | **批 13 的新代码（`lib/**`）需重启才生效** | **应已生效**（同上，静默面） |
> | ③ | **`consult` 三处工具描述的修订（`37d7eef`）** —— **★ 其结论已被批 15 反转**（consult 现走平台 `jobs` ⇒ **有通知了**）⇒ **以批 15 的描述面为准**，`37d7eef` 的文本已被批 15 的 `FR-2` 逐字替换 | **已被批 15 取代** |
> | ④ | **批 15（v0.22.0）** = consult 改走平台 job 投递 + **退役 `consult_check`** + 消化门禁与纪要落档（`docs/consult-minutes/`） | **应已生效**——**可感变化：会诊结束自动被通知；`consult` 工具家族 3 → 2** |
> | ⑤ | **批 14（v0.23.0）** = `lib/eng.mjs`（**默认值 `medium`** + 三处到点文案 + 前置告警）· `lib/config-store.mjs`（头注释补 `runner`）· 另五档注释（删 10 处悬空引用） | **★ 已实测生效**（见下） |
>
> **★ ⑤ 的实测证据（父侧 2026-09-16 核查）**：`ENG_CODER_EFFORT_DEFAULT` 常量**在位**且值 = `"medium"` · **旧硬编码 `let engCoderEffort = "low"` 零残留** · **三处第一字面全带 `codexCli.` 前缀** · `PLATFORM_WALL_CLOCK_MS` 在位 · 安装点（junction）与工作树 **sha256 一致**（`1CB5E990…`）。**⇒ 可感变化：新部署的默认推理档从 `low` 变成 `medium`**（`low` 在不支持它的模型上会静默落到 `off` = 推理全关）。
>
> **★ 新增一件不需要重启的（批 16，v0.24.0）**：**四条文档形状谓词并入 `test/doc-hygiene.test.mjs`**——**`lib/**` 零改动 ⇒ 纯测试面 ⇒ 不需要重启**。**它的生效方式就是 `node --test`**（464/464），**没有运行时面**。**⇒ 至此「待重启」清单清空**（五件已随本次重启消化，批 16 不需重启）。
>
> **★ 同样不需要重启的（批 17，v0.25.0，2026-09-17）**：**事实 id 跨档等值腿**（把判据从「某张表内部自洽」升为「**同一可数事实的每一处出现彼此同值**」）＋ **三处既有正确性修复**（深度感知闭括号 · 围栏掩码 · 掩码改非空白占位符）——**`lib/**` 零改动 ⇒ 纯测试面 ⇒ 不需要重启**；**生效方式同样是 `node --test`**（**476/476**）。**★ 可感变化**：**新写的批次档若在 `docs/` 顶层且文件名以日期开头、日期 ≥ `2026-09-16`，就必须带 `doc-shape` 声明**（否则谓词红）——**存量档全在切点前 ⇒ 零触动**。**★ 还有一件被实测否掉的**：会诊曾建议「让腿 1/腿 3 全档常开」，**父侧干跑 62 档实测腿 3 常开会让存量档红 26–34 处**（裸模块名）⇒ **不采纳**，**故不会出现「升级后存量档变红」**。
> **★ 需要重启的一件（批 19，v0.27.0，2026-09-17）**：**`lib/effort-resolve.mjs` 的开关语义根修** ＋ `lib/eng.mjs`（**仅注释**）⇒ **`lib/**` 有改动 ⇒ 重启 DSH 后生效**。**可感变化**：① **请求 `low` 而模型支持集是 `{off,high,max}` 时，不再被静默关掉全部推理**（改为落 `high`；**本部署 20 个模型条目里 8 个是该形状**）；② **显式配 `off` 而模型关不掉时会被明说**——省略该参数、交还提供方默认，**而不是悄悄换成某个力度档**（旧行为会把它改成 `low`/`medium`/`max`，即「用户要关推理、却被打开」）。**⇒ 生效方式只能是重启**（`node --test` 证的是代码正确，证不了运行时面）。**本部署今天未中招**：user 层的 `engCoderEffort` 是 `high`、convergence 是 `max`，覆盖了 profile base 里那对「`glm-5.3-flash` + `low`」的中招组合。**★ 另登记一条流程残差**：本批的**分歧审计子代理被父侧中断、未产出报告** ⇒ 独立验证面比批 14/16/17 薄；对策见 `docs/NIGHT-SHIFT-PLAN.md` 的批 20 行与 `consult #17` 纪要 §4。
>
> **另**：用户另设了 `codexCli.budgetCapMs = 1800000`（30 分钟）——**它超过平台 `run_code` 默认墙钟 600s**，代码里的 **D-16 不变式提醒**正为此存在（`advisor.mjs` 的 `jobsDispatchReply`：`budgetCapMs ≥ 600000` 时追加「确认已同步提高平台 maxWallMs 且 cap < wall」）；**插件读不到 `maxWallMs`（无注入面）⇒ 只能提醒、不能强制**。**⇒ 长任务的正解是传 `background: true`（见 README 新节），不是调 `budgetCapMs`**。

**★ 哪些面已机检、哪些仍是人工（下个会话必读，防误以为全覆盖）**：批 10 的「计数有据」闸（`test/ledger-parity.test.mjs`）**只罩两个面**——**缺陷登记表**（自报 36 条）与**吸收清单 §2**（自报 `共 42 档`）；外加 `test-lifecycle.test.mjs` 罩**测试台账 §三**（**20** 档用例数，等值锁）。**批 11 起新增第三个机检面**：`test/doc-hygiene.test.mjs`——**全仓 U+FFFD = 0**（根 + `docs/` + `lib/` + `test/` 的 `*.md/*.mjs/*.js/*.json/*.txt`，落地前实测 0 ⇒ **零白名单**）+ **五常设档 canary**（防截断为 1 行）；批 11 的锚腿 V1–V12（`METHODOLOGY.md` 新子节形状/零状态句/`lib/eng.mjs` 后缀块挂点/两档提示词正向锚/`lib/index.mjs` 划界）也在该档内机检。**其余全部是人工**，其中**交接页是刻意的人眼闸**（`release-check` G7 MANUAL，理由是「活页、编号会漂、无机器契约」——评审 #3 的正式收窄，**不要因为「它漂过」就翻案**：批 10 自己就在交接页漂了一次 447→449，仍按人工处置）。**已知会漂的人工面清单**：交接页自报计数 · `CHANGELOG` 顶部计数（由发布门 G3 在 **tag 前**抓）· 各档的「状态」行 · 各档自述里的枚举与计数（本会话「自报计数漂移」累计 **12 次**命中，其中 6 次发生在批 10 自己身上、1 次发生在批 11 的 §2:84 并已随本批订正）。

---

## §2 每批要走的那条链路（**逐条都吃过亏，别省步骤**）

`会诊 → 纪要（含分歧 + 父侧裁定）→ 需求档 + 设计档 → 设计评审 → eng_coder 实施 → 分歧审计 → 交付代码评审 → 提交`

| # | 纪律 | 来源（吃过什么亏） |
|---|---|---|
| 1 | **设计档必备九节 + 涉及流程/数据流必配图** | 用户 2026-09-12 裁定，所有批次适用 |
| 2 | **锚的谓词必须写全三件事**：何时点（历史 vs 工作树）· 何谓改（新增/修改/删除分开）· **自指**（锚所在文件自身是否在集合内） | 三次栽在同一坑：批 2「零新增用例」被审计揭穿 → 批 4 T-G9 → 批 6 T-AP9（**登记 D-37**） |
| 3 | **判定族字面只追加、绝不替换** | 批 6 设计评审轮次 1 的 🔴：设计档自相矛盾（「追加」vs「替换 + 逐处改断言」），而零改面纪律禁止改断言 |
| 4 | **禁止用 PowerShell 做代码文件的读-改-写**（`Get-Content -Raw` 按 ANSI 读 UTF-8、`WriteAllText` 写 UTF-16 ⇒ 乱码 + 丢换行）。脚本级改写一律 **node 显式 `utf8`**，写后核 `U+FFFD` = 0 | 批 3（父侧本人）与批 6（实现者）**两次同因事故** |
| 5 | **交差验证是常态**：设计评审 → 实施 → **独立分歧审计**（explore 子代理）→ 交付代码评审（advisor code）→ 修复轮。**审计抓到的真缺陷已经有 4 个**（批 4 的 codex 中断计振、批 4/5 的工具层与 CJK「假锁」、批 6 的「手搓等价物 + 无消费者」🔴） | — |
| 6 | **设计评审的发起权**：默认在用户（我呈递就绪并提醒，不自己开）；**用户显式授权时可代签**（2026-09-13 夜即此模式，已记入 METHODOLOGY.md） | ENGINEERING-MODE 规则 |
| 7 | **每两次设计评审之间插一次代码评审** | 否则同一会话里的第二份设计文档会拿到**收敛轮**提示词（只验证、不找新问题）+ 别人的 prior ⇒ 令牌声称覆盖的文档从未被真正评审（**D-35**，已修） |
| 8 | **`eng_coder` 必须传 `designToken`**（参数传，不写进任务文本）；大任务传 `stages`；预计 >9 分钟用 `background: true` | 平台 600s 墙钟 |

---

## §3 环境事实（不知道这些会浪费一整轮）

| # | 事实 |
|---|---|
| 1 | 插件工作副本 = **实时插件**（web profile 里是 junction 指向 `D:\DSH-Portable\plugins\dsh-thincoder-suite`）；**改 `lib/**` 或提示词需重启 DSH 才加载**（模块在启动时读入）。改 `docs/**` 立即生效（我在读盘）。 |
| 2 | `designToken` 是**会话内存态**（`session-state.json` 刻意不持久化它）。**DSH 重启后内存态为空**——**批 10 起（D-36 已修）：写门禁在内存态为空时自己读盘**（三态路由：本会话有效盘记录 ⇒ 放行 + 回填 + 落一行 `gate: design token restored from token-store` 日志；过期 ⇒ 复用 expired 文案；无记录/他会话 ⇒ 复用 no-token 文案；**读盘失败 ⇒ 拒**）⇒ **重启后主代理可直接做已批准设计的小修，不必再过一次 `eng_coder`**。`eng_coder` 的磁盘回填路径（收窄为「传入 token === 盘上记录」）**原样保留**。**登记 D-36**（已修，见 §4）。**2026-09-13 批 9 实测（历史，仍有效）**：重启后**用原令牌值重发 `eng_coder` 成功了**——磁盘回填路径本来就在（这正是 D-36「内存态不是能力边界、只是摩擦」的能力论据）。 |
| 3 | **DSH 大约每 2 小时重活后因资源耗尽崩一次**（2026-09-13 三次：01:49 / 11:27 / 18:41，均为原生失败 + 「虚拟内存/系统资源不足」）。**建议在批次之间的空档重启 DSH**，并关掉底噪大户（ChatGPT 桌面 ~1.3 GB、微信 ~0.4 GB）。机器总内存 15.76 GB。 |
| 4 | 会诊/评审都跑在 `ctx.jobs` 后台 job 里（**进程内**）。崩溃会**连带杀掉在飞 job**，job 注册表随之销毁（`job_output` 查不到）。所以**每完成一步就落盘**是本仓的生存法则。 |
| 5 | 设置页的「生效值」来自 **user 层 > base**；`cordis.patch.yml` 只是 bundle 缺省。 |
| 6 | 测试：`node --test`（零依赖、零网络、零真实 LLM）。**批 10 交付后基线 449/449**（批 9 为 434、批 8 为 413、批 7 为 399、批 6b 为 384；需求档 §0 提到的 360 是批 6 当时的基线，已过时）。**注**：本档是**刻意的人眼闸**（`release-check` G7 MANUAL），**不进**计数有据的机检面 ⇒ 其自报数字靠人工同步（批 10 曾在此漂过 447→449，由补交轮发现）。 |
| 7 | **交付/发布模型（用户 2026-09-13 裁定）**：本插件**靠 git tag 发布**，**不上 npm**（`package.json` 标 `"private": true`）；**别的机器从 github 拉取**自建。本机形态是 **junction 挂载**（`profile\profiles\web\node_modules\@dsh-external\dsh-thincoder-suite` → `D:\DSH-Portable\plugins\dsh-thincoder-suite`）⇒ **改完重启 DSH 即生效，无发布步骤**。**仓内无 CI、无 `scripts/` 目录**。⇒ 凡涉及「发布门/集成集/runner」的机制，**判据必须按 tag 模型重写**，不能照搬上游的「npm publish + VSC/CLI 多目标」形态。 |
| 8 | **批 7 的部署注记仍未生效**（2026-09-13 核实）：`$DSH_HOME/.thincoder/config.json` 的 user 层里 `advisor.standardsDoc` / `advisor.documentMapDoc` 均为 `undefined` ⇒ 本仓设计评审当前走「**未声明**」的显式降级句、**不注入标准判据**。**注意**：配置不是热更新——PUT 落盘后需**下次重启**才被合并读取。 |
| 7 | **批 7 的部署注记（dogfood，AC-P17）**：批 7 把「评审按什么标准判」从**写死文件名**改成**项目自己声明**（键 `advisor.standardsDoc` / `advisor.documentMapDoc`）。本仓要保住批 7 之前那份标准注入，**必须在设置页（或 `$DSH_HOME/.thincoder/config.json` 的 user 层）配**：`advisor.standardsDoc = "METHODOLOGY.md"`、`advisor.documentMapDoc = "docs/README.md"`（两个值都相对**会话 cwd**）。**不配 = 注入器走「未声明」显式降级句** ⇒ 本仓设计评审会丢掉标准判据（属回归性变化）。清空文本框 = 发送空串 = **撤销该键**（回到未声明态）。改 `lib/client.js` 后**需重启 DSH** 设置页才出现这两个文本框。 |
| 9 | **批 11 的三条「待重启观察」（本进程内不可验）**：① 运行中的 DSH 会话**看不到**新提示词条文与新返回串（该进程已装载旧 `lib/**`）；② 真实宿主子代理 abort 的**端到端**路径（测试只验字串构造）；③ 父 agent **遵守**「不自动重派」与卫生五条（提示词级纪律只有**字节锁**可验）。⇒ 本批一切 AC 措辞为**字符串/结构断言**、**无行为断言**（设计档 §11；「待重启观察」= 本条）。 |

---

## §4 未决缺陷与登记项

> **引用约定（批 10 成文化，D10-16）**：引用本表条目（以及登记表 `docs/2026-09-05-defect-registry.md` 的条目）
> **只用 ID**（`D-31` / `R-13` / `O-E1`…），**不复制标题、不复制状态、不复制指针/触发**——
> 「每个事实一个家」：状态与指针/触发的家**在登记表**，本页只是快照。
> ★ 本页 §4 **刻意不加列、不加机检**：它是取词面的人眼闸（对应 `release-check.mjs` 的 G7 MANUAL）。

| # | 级别 | 内容 | 状态 |
|---|---|---|---|
| **D-31** | 🟡 | **未知顶层键被静默丢弃却返回成功**（`validateGlobalUserConfig` 把未知顶层键归 `notes` 而非 `errors`）——**真实严重度 = 全未知键 PUT ⇒ 200「已保存」而 user 层被整体替换清空**（批 10 父侧实测复现，R-21 订正） | **已修（批 10）**：未知顶层键移入 `errors` ⇒ PUT **400** ⇒ **写盘之前拦下**；独立回归锚 = 「全未知键 ⇒ 400 且盘字节不变」+ 客户端 ⊆ 服务端白名单锁（D10-10） |
| **D-36** | 🟡 | 重启后写门禁拒绝主代理小修（见 §3-2） | **已修（批 10）**：`makeWriteGate` 在**内存态为空**时显式读盘（显式三态路由：valid ⇒ 放回 + 留痕；expired/missing ⇒ **复用**既有两条文案）；内存路径逐字不动、读盘失败 ⇒ 拒、docHash 不进门禁判据。**推翻既有裁定 D1** 已设专节（设计档 §10）+ `eng.mjs` 注释同步改写 |
| **D-31/D-36 的台账面** | — | 登记表已补「指针/触发」两列（36 行）+ 计数订正（自报 33 → **36**，与机械行数一致）；吸收清单计数归一（43 → **42**，`共 N 档` 单点） | **已随批 10 收口**（`test/ledger-parity.test.mjs` 机械钉死：改任一侧即红） |
| **D-37** | 🟡 | 零改面锚的「工作树 vs HEAD」形态不可持久 | **批 6 已修**（T-G9 与 T-AP9 都改成持久形态）；规则已提炼进登记表 |
| **R-1**（批 6 登记，**交付代码评审后扩围**） | 🔵🟡 | **codex 面诊断闭环**（三件同族，一并裁定）：① codex job 的 `ABORTED` 信封路径无 `abort(...)` 标签；② codex **同步**路径 catch 只在 `if (signal?.aborted)` 臂接死亡行——适配器以 **AbortError 抛出而调用方信号未中止**时回落通用文案（**同族谓词不对称**：dsh 路径的谓词是 `signal?.aborted \|\| e?.name === "AbortError"`）；③ codex **后台 job** 的 reject 回调同样未接 | **下一个碰 codex 面的批次**裁定（建议与批 6b 或批 7 合并处置；父侧 2026-09-13 显式裁定**延后**，理由：同族统一裁定更不易漏 + 不阻塞任何 AC + 用户已裁定收尾换会话） |
| **R-2**（批 6 登记） | 🔵 | codex 面测试构造有实现耦合点 + 遗留 OS 临时目录（测试侧副作用） | 登记 |
| **R-3**（批 6 登记） | 🔵 | T-AP9 锚 A 提交前「未激活」（warn 而非静默通过） | 已随提交生效 |
| **R-4**（批 6 交付评审登记） | 🔵 | 两处测试健壮性微修：① `T-AP7` 的 git 交叉核验只容忍 `ENOENT`（git 在装但目录不在仓库内 → exit 128 直接红），应像 T-AP9 锚 B 那样 warn+skip；② `abort-provenance.mjs` 的 `deathLine` 末尾 `return raw.slice(0, DEATH_LINE_MAX)` 是不可达死代码（无害） | 登记（测试健壮性微修批） |
| **O-E1**（批 6b 登记） | 🟡 | **跨会话误拒**：预闸**扫全表** ⇒ 会话 B 写会话 A 冻结集内的文档被拒。爆炸半径 = 有界窗口内的一次工具调用；逃生门齐备（`job_kill` / 等 settle） | 登记（采样后若成实测痛再收窄为按会话键查） |
| **O-E2**（批 6b 登记） | 🟡 | **跨链过度冻结**：令牌生命周期内 `pendingDocPaths` 单调扩张（只在重铸时归零），链切只重置轮次/prior **不动**令牌与并集 ⇒ 转链后 B 的窗口仍冻结 A 的文档。**D-30「并集永不忘」既有语义投影进窗口**，非批 6b 引入 | 登记（修法属 D-30：链切换即重铸） |
| **O-E3**（批 6b 登记） | 🟡 | **未过期旧令牌 + 文档漂移 → `eng_coder` 直接放行**：续期校验只在**过期**子分支跑指纹，spawn 时不复检。同族 **F17 类洞、窗口外** | 登记（属 D-30 扩展） |
| **O-E4**（批 6b 登记） | 🔵 | `file_ops` 条件纳入预闸（schema 未核实；若有稳定 `{op, path}` 形态，扩枚举是独立设计变更） | 登记 |
| **O-E5**（批 6b 登记） | 🔵 | **路径 cwd 偏斜**：`normalizeDocPath` 以 `process.cwd()` 为基，评审传相对路径 / 写传绝对路径 ⇒ 预闸 **miss**（非误拒）；miss → 结算侧兜底捕获。deny 文案展示**归一后**集合供排查 | 登记（**严禁**为修偏斜自造 cwd 归一，会劈开判据单点） |
| **O-E6**（批 6b 登记） | 🔵 | `computeDocHash` 对「目录取代文件」「权限变更」等只报 `ok:false`，不区分原因 | 登记 |
| **O-E7**（批 6b 登记） | 🟡 | **计振裁定的未决风险**：若将来出现「带外写入者每窗必漂 → 无限免费重评」，解法是**独立漂移计数器**（与三振正交、各有其出口），**不是**把漂移塞进三振词表 | 新批次（纪要 §2-3 / 设计档 §12.6） |
| **R-5**（批 7 登记） | 🟡 | **六维 / 七维清单预先存在的漂移**：`prompts/advisor-design.md` 列 **7 维**（含 Document ownership），而 `advisor-msgs.mjs` 的硬要求句只列 **6 维** ⇒ 系统提示要求一个硬要求句未列的维度。批 7 **只换措辞不并维** | 登记（待裁定：并维，还是改系统提示） |
| **R-6**（批 7 登记） | 🔵 | `advisor-msgs.mjs` 读 `<cwd>/.thincoder/advisor.md`——沿用上游产品名作目标仓约定路径（有 `DEFAULT_CRITERIA` 兜底，非静默缺陷） | 登记（后续批考虑键化） |
| **R-7**（批 7 登记） | 🔵 | `viz/*.html` 内含过时数字——一次性分析产物，不动 | 登记 |
| **R-8**（批 7 登记） | 🔵 | `index.mjs` eng_coder 工具参数描述里的示例路径 `lib/x.mjs` 略带本仓形状 | 登记（可泛化为 `<file>`） |
| **R-9**（批 7 登记） | 🔵 | `cordis.patch.yml` 的白名单注释已过时（批 5 加 `contextTokens` 时未同步该注释） | 登记（同物种文档漂移） |
| **R-10**（批 8 登记） | 🔵 | `Never translate this token` 一行的 **3:1 措辞差**（`advisor-design.md` = `the host parses this line literally`，其余三档 = `the line is parsed literally`）——**已登记于 `lib/prompts/verdict-contract.md`，不修**（修则违「判定族字面只追加」） | 登记 |
| **R-11**（批 8 登记） | 🔵 | 判定族的**出现点分布**：`must-fix` 例外句全族 **6 处**（四档各 1 + `advisor-msgs.mjs` 2）· 次序句全族 **3 处**（`advisor-design.md` 1 + `advisor-msgs.mjs` 2）——与 `VERDICT_LINE_RE`（`advisor.mjs`）构成「**契约单点 + 文本多处**」结构 | 登记（单点化留待专门批次） |
| **R-12**（批 8 登记） | 🔵 | 吸收清单 `COMMON-LAYER` 行记载的重复症状（`engineering.md:173-174` 重复标题）**已陈旧**——实测无重复标题，全库唯一跨档重复标题就是那 4 条 `## Verdict Line` | 登记（已更正记载） |
| **R-13**（批 8 登记） | 🔵 | **`test/session-state.test.mjs` T6（TTL 清扫）的既有时序 flake**：一次全量中失败（`actual ['keep','malformed','stale1','stale2']` vs `expected ['fresh','keep']`），单独复跑该档 **10/10 绿**、其后全量**连续 7 次绿**；不在批 8 写域、与其 diff 无因果 | 登记（批 9 裁定**不根治**：该档属基线 10 档，改它撞 T-AP9；发布门 G6 已写死预案。后续批次排查：疑似并行下 TTL 时钟竞态） |
| **R-14**（批 9 登记） | 🔵 | **宿主复跑 stage `check` 的观察位**：出现「子代理申报与实测不符」的实测案例即立项评估（同时登记复跑的三项新成本与第 8 步重叠论证——这是「不现在做」的依据，不是「永不做」） | 登记 |
| **R-15**（批 9 登记） | 🔵 | `codex-cli:gpt-6-astra` 在批 9 会诊 `PROCESS_ERROR`（非零退出且无有效输出）——**如实登记不补位** | 登记（与批 4「2/4 交付」同族） |
| **R-16**（批 9 登记） | 🔵 | 会诊把上游路径写错（`src/agent-tools/completion.mjs`，实际 `src/agent/completion.mjs`）——**引用内容为真、路径记错** | 登记（教训：**会诊给的路径也要回盘核**） |
| **R-17**（批 9 登记） | 🔵 | 交接页曾有**重复的批 9 行**（一行带 `verify` 半件、一行不带） | 已由父侧合并 |
| **R-18**（批 9 登记） | 🔵 | 台账的退役判据 R1–R4 与反 tautological 四律，其**自身语义正确性**只能靠评审人眼——机械只保形式（**诚实边界，已写入台账与需求档，不许超卖**） | 登记 |
| **R-27 · R-28**（批 11 登记） | 🔵 | 见 [`2026-09-13-doc-discipline-requirements.md`](./2026-09-13-doc-discipline-requirements.md) §5.2——按本页引用约定**只留 ID，不复制标题/状态** | **R-27 已随批 11 闭环**（吸收清单 §2:84 的「共 4 条」→ **3 条**、`:39`/`:40`/`:57` 三行触发列标 `—（已闭环）`、变更理由行在吸收清单 §6）；**R-28 登记**（评审员末行 `VERDICT:` 的写法，已进后续派单口径） |
| **批 11 交付期发现**（实现者报、**父侧已裁定并修复**） | ✅ 已处理 | **设计档内部两处互相否定（本会话第六次同型）**：`2026-09-13-doc-discipline-design.md` §8.2 的 **V5** 逐文件负向 grep 清单含「`METHODOLOGY.md`（新子节）」，而同档 §6.1 的**逐字交付** D1 行自指其档名 ⇒ **照 §6.1 交付则 V5 该腿必红** | **父侧裁定（2026-09-13）**：**V5 的清单只列 `lib/**` 内的落点文件**；**`METHODOLOGY.md` 不进 V5**——它是**常设标准档**，其六串守护者是**仓库级 T-PK15**（`walkLib()` 递归 `LIB_DIR` ⇒ METHODOLOGY **从来不在其扫描面内**），且六串禁令的**意图**是「新写的**产品面**文本不得复活被退役的指涉」（§9 边界 5 的措辞本就是「**提示词**里的六串」）。**未改 §6.1**（逐字交付文本不动）。**实测**：`lib/**` 内六串精确谓词命中 **0**；`METHODOLOGY.md` 的 3 处 `/methodology/i` 均为**正当内容**（档案标题 / 写权矩阵自指 / 历史行），**不被任何锚禁止**。**完整落档见设计档 §14.7**（本行初写「§14.6」——**D4 指针纪律要治的病，出现在同一批的交付物里**；父侧已订正） |
| **R-33 · R-34 · R-35 · R-36**（批 13 登记） | 🔵 | 见 [`2026-09-15-registry-criteria-requirements.md`](./2026-09-15-registry-criteria-requirements.md) §5.2——按本页引用约定**只留 ID，不复制标题/状态** | 登记（**R-34／R-35 是跨批次约束**：前者「`self-consistency ≠ protection`」后者「全局钩子拿不到会话身份」） |
| **R-38**（批 14 立项期登记，**父侧 2026-09-15 实测**） | 🟡 | `engCoderEffort` **默认值 `low`**（`eng.mjs:750`）在一个**不支持 `low` 的模型**上**必然静默降级到 `off`**（`effort-resolve.mjs:99-100` 的回落 ⇒ **推理预算全关**），而 **README 让用户照抄的正是 `low`**；上游 2026-09-04 只修了「加回落」，**没修「默认值在一个不支持的模型上必然降级」**。**实测佐证**：本机 eng_coder 每次都打 `effort "low" is not supported by model glm-5.3 (supported: off\|high\|max) — falling back to nearest supported effort "off"` | **登记 = 批 14 立项范围**（配置面与描述面同步；批 14 启动时先做摸底确认真实现状）。**★ 用户可用性提示（不需改代码）**：把 `engCoderEffort` 改成 `high`/`max` 立即生效（**用户 2026-09-15 已改为 `high`**） |
| ~~**R-37**~~ → **已撤回**（**父侧 2026-09-15 自纠**） | — | **登记时称「设置页没有 `budgetCapMs` 输入框」——实测为假。** 该输入框在 `client.js:1316`，label 逐字写着「budgetCapMs（codex 预算上限，**须低于 run_code maxWallMs**）」，且 `:309`/`:382`/`:458` 三处都在处理它。**父侧第一次检索未命中**（那次只覆到 `codexCli` 的另外几个字段），**据错结论登了记** | **本条撤回。** 留痕：**这是本会话「登记描述出错」的又一例，而这次出错的是父侧自己**（与 **R-33** 同族）。**教训**：对「某 UI 面**不存在**」这类**否定性断言**，必须**用检索证明其不存在**，而不是「我没搜到」——**否定性断言是登记里最容易错的一类** |

---

## §5 已就绪的设计输入（**不必重新会诊**）

| 批次 | 输入 | 出处 |
|---|---|---|
| **批 6b 守卫 E** | 会诊 id 1 **两份独立裁定**都覆盖了它：窗口起点 = 在飞槽位登记、终点 = settle；拦截面 = 独立的 `tools/pre-execute` 监听器（**不并进写门禁**——两者 fail 方向相反）；判据与 `computeDocHash` **同源**；**逃生门 = `job_kill`**（DSH 原生，确实存在——v4-pro 当时误判为「没有」）；**glm 的补强 = 结算侧指纹兜底**（design PASS 时在 finalize 内重算文档集指纹，失配则不签发——补上预闸拦不住的 bash/file_ops 写面）；槽位载荷扩容 `{jobId, mechanism, reviewType, docSet}` | 会诊 id 1 的两份回复（在旧会话里；**要点已摘入本表**） |
| **批 7 可移植性三件** | 只读勘察已给出**逐处清单**（**旧快照——已由批 7 会诊 id 2 + 两轮只读勘察取代**）：`METHODOLOGY` 命中 = `advisor-msgs.mjs` 8 处 · `engineering.md` 9 处 · `advisor-design.md` 2 处（**合计 19 处**）；**`check-doc-width` / `docs/TODO.md` 在提示词里零命中**。终态口径（上游 PORTABILITY）：**指令文本改写成「评审上下文提供的项目标准」**（不是整段删除）、注入器改为**声明键 + 显式降级句**、**自指脚本类整个删掉**。判据单一权威 = `isProductCode`（`eng.mjs`）与 `isDocFile`（`advisor.mjs`）**两份互相矛盾的副本** | 只读勘察子代理报告（要点已摘入本表）。**⚠️ 本行已被批 7 的设计输入取代**——正式输入见 [`2026-09-13-portability-consult-minutes.md`](./2026-09-13-portability-consult-minutes.md) + [`2026-09-13-portability-requirements.md`](./2026-09-13-portability-requirements.md) + [`2026-09-13-portability-design.md`](./2026-09-13-portability-design.md)。**父侧在本行踩过的坑**：① 「两份互相矛盾的副本」**不准确**（实测唯一矛盾类 = 根 `src/` + 文档扩展名）；② **`docs/` 对两谓词语义相反是错的**（都归文档侧，该说法已撤回）；③ 配置面实测是 **7 个面**不是「四面管道」 |

---

## §6 新会话的第一小时建议

1. 读本页 + `docs/2026-09-12-absorption-inventory.md` §6 + `docs/2026-09-05-defect-registry.md`（未决项）。
2. `node --test` 确认基线（**应为 453/453**——批 11 交付后的值；批 10 为 449）；`git log --oneline -5` 核对 §1 的提交在不在。
3. **批 11 已实施**（D1–D7 文档纪律 + 上游 ENGINEERING-MODE 失败路径语义 + DOC-HYGIENE 提示词级纪律，见吸收清单 §6；**§6 的批 11 行已由批 10 补齐、批 11 stage 7 补变更理由行**）；**待**独立分歧审计 → 交付代码评审 → 提交。**批 6b / 7 / 8 / 9 / 10 均已完全收口**。
4. 若写门禁报「no design token」→ 按 §3-2 处理（过一次 `eng_coder` 回填；或跑一次设计评审）。
5. **批 7 提炼的三条硬纪律 + 批 8 追加的第四条**：
   - **① 裁定写进纪要 ≠ 设计档兑现**：批 7 设计评审轮次 1 的 5 条 🔴 里 **4 条**是「纪要有裁定、设计档漏掉或 override」（J8 地图机制 · J11 路径前置校验 · 纪要 §3-③ 卫生锁 · 我规定的字串撞自己的闸）。**设计档交付前必须逐条回读本批纪要的裁定清单做交叉自检**，不能靠「我写过就默认它在里面」。**批 8 又命中一次**（纪要写明闸 = 双向一致 **+ 内容指纹**，设计档只落前者）。
   - **② 改了引用点，却没改被引用的副本**：轮次 2 的 7 条残留里 **4 条**是「同一件事在文档里有第二个落点」。**纪律 = 每次改一个约定，必须全库检索该约定的所有出现点并一起改**——这条 `grep` 级纪律原先只当**代码面**纪律用，**文档面同样适用**。
   - **③ 不许用「不可达」当免验理由**：「不可达」本身是需要证明的断言。
   - **④ 给用户/实现者选项前，必须先用工具验证每个选项真的可行**（批 8 的 T-AP9 教训：我第一次问测试落点时漏算了 T-AP9，用户基于不完整信息选了实测不可行的那条路，回盘实跑证红后才改判）。
