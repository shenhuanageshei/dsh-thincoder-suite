# 需求：谓词作用域与宣言对齐 —— 批 17

- 日期：2026-09-16
- 前序：[`2026-09-16-doc-shape-design.md`](./2026-09-16-doc-shape-design.md)（批 16，`v0.24.0`）——本批是它的**直接续作**
- 章节：§0 索引 · §1 总目标 · §2 背景与问题 · §3 用户故事 · §4 非功能标准 · §5 边界与登记面 · §6 历史行
- 状态：**需求待评审**

---

## §0 索引

| 要什么 | 去哪 |
|---|---|
| **本批立项的五条实测证据** | 本档 §2.2（逐条带批 16 的评审/审计出处） |
| **批 16 交付了什么、作用域为何这么窄** | [`2026-09-16-doc-shape-design.md`](./2026-09-16-doc-shape-design.md)：§5.1（声明语法）· §8.2（锚 A1–A10）· §12.2（审计结论） |
| **批 16 的失败统计** | [`METHODOLOGY.md`](../METHODOLOGY.md)：批 16 交付记录（「五次复发」段） |
| **四条腿的现有形态** | 批 16 设计档 §5.2–§5.4 |
| 仓库的文档纪律 D1–D7 | [`METHODOLOGY.md`](../METHODOLOGY.md)：§文档纪律 |

---

## §1 总目标（一句话）

**让批 16 交付的形状谓词，看得见它此前看不见的那些面**——**判据从「两张表对不对」升为「同一事实的每一处出现是否同值」**，**而这一步的正确性同样由机器证明，不靠人眼**。

---

## §2 背景与问题

### §2.1 立项来源：批 16 分歧审计的一句结论

> **「谓词不是错的——是它的作用域与它的宣言不匹配。」**

批 16 交付了一个**能工作**的形状谓词（四条腿 · 九道闸 · 八条负控 · 464/464），**但它只看两处**：**档内声明的锚表与 AC 表**。**⇒ 而它的失败统计显示，五次「同一事实两处取不同值」全部发生在别处。**

### §2.2 五条实测证据（批 16 全周期，逐条带出处）

| # | 形态 | 发生场所（**谓词看不见**） | 发现者 |
|---|---|---|---|
| **P-1** | 只改了 AC-10 行，**A10 锚行未动却宣称已改** | §8.2 的**锚行本身** | 批 14 设计评审轮 2 #1 |
| **P-2** | 订正语**留在列表格里** ⇒ 机器读到的仍是字面 token | §10.2 的**列表格** | 批 16 分歧审计 F1 |
| **P-3** | **US-5 与 §7/§10.1 漏改**（三处口径不一致） | 需求档 §3 与设计档 §7 / §10.1 | 批 16 分歧审计 F3 + 代码评审 #1 |
| **P-4** | **修复轮漏改 `CHANGELOG.md`**，且零留痕 | **`CHANGELOG.md`**（不在 `docs/` 下） | 批 16 代码评审第 2 轮 #1 |
| **P-5** | 收口时计数行**一节之内两处推导式**（应 `456+8`，写成 `456+5`） | `CHANGELOG.md` 首节 | **仓库既有的 `G-常驻3`** |
| **P-6** | **枚举列 8 项却断言「9 条构造」** | §8.2 的**计数订正行** | 批 16 代码评审第 3 轮 #5 |

**⇒ 6 条里 5 条由独立评审发现、1 条由既有解析器发现；父侧自查 0 条。** **⇒ 而它们全部落在谓词作用域之外。**

### §2.3 ★ 结构性观察：为什么「扩几处」是不够的

**批 16 的形态是「谓词只扫声明过的表」**，**而缺陷分布是「凡是有计数/枚举的地方都可能漂」** ⇒ **按面逐个扩是打地鼠**：**扩到 §7，下一批的漂移会出现在 §9；扩到 CHANGELOG，下一次会出现在 README。**

**⇒ 本批要换的不是「多看几处」，而是「判据的形态」**：**从「某张表内部自洽」升为「同一可数事实的每一处出现彼此同值」。**

---

## §3 用户故事

| # | 用户故事 | 判定句 | 可验层 |
|---|---|---|---|
| **US-1** | 作为**写档的人**，我希望**任何一处「N 项 + 列表」都被核对**，而不只是声明过的表 | 谓词的计数核对**不再要求「位于声明的表内」**——**档内任意处的计数标记都参与**；**仍只认显式标记**（不回归到正则猜中文） | T2 |
| **US-2** | 作为**审阅者**，我希望**同一个可数事实在多处出现时能被自动比对** | **同一事实的多处计数值必须同值**（**事实的声明方式见设计档**）；不一致 ⇒ 红，且**报出全部出现处** | T2 |
| **US-3** | 作为**验收者**，我希望**未声明的面不再静默逃出作用域** | **进作用域 = 档内自带声明**（批 16 的机制保留）；**但声明必须声明得「全」**——**「哪些面要扫」本身有默认值**，**不声明 != 不扫** | T2 |
| **US-4** | 作为**维护者**，我希望**批 16 的四条腿一条也不退化** | 批 16 的 **AC-1…AC-12 全部仍绿**；**八条负控腿全部仍红** | T1/T2 |
| **US-5** | 作为**审计者**，我希望**新能力自身被证明会红** | 每条新腿**至少一条负控腿** + **阴性对照**（防恒真 / 防误伤） | T1 |
| **US-6** | 作为**读档的人**，我希望**`CHANGELOG.md` 也被覆盖**（它不在 `docs/` 下，却是登记面） | 覆盖形态见设计档（**它是另一种文件，不能套 `docs/` 的声明机制**） | T2 |

---

## §4 非功能标准

| # | 标准 | 判定 |
|---|---|---|
| **N-1** | **不回归到「正则猜中文」** | 批 16 实测：猜的形态**误报 62%**（24 处判不等 15 处，根因 = 中文量词无词边界）⇒ **仍只认显式标记** |
| **N-2** | **不追溯存量**（批 16 的 D16-2 继续有效） | 谓词**只作用于自带声明的档**；**存量 53 档零触动** |
| **N-3** | **批 16 的四条腿与八条负控一条不退化** | 464/464 中的批 16 用例全绿；负控全红 |
| **N-4** | **每条新腿有负控腿 + 阴性对照** | 批 16 的纪律（D16-6）沿用 |
| **N-5** | **不新增测试档**；并入既有 `test/doc-hygiene.test.mjs` | 台账 §三计数同批同步（`T-LC2` 等值锁） |
| **N-6** | **锚写全三要素**（检索目标 / 谓词 / 期望） | 批 15 审计 W3 的教训 |
| **N-7** | 零新依赖 · 不擅自重启 · 不擅自改用户配置 | 常设约束 |

---

## §5 边界与登记面

### §5.1 本批不做

| # | 不做项 | 理由 |
|---|---|---|
| 1 | **覆盖 `README.md`** | 它面向用户、历史包袱重（**批 16 收口时刚补过版本表**）；**且它与 `docs/` 的形态完全不同** ⇒ **先在一个不在 `docs/` 的登记面（`CHANGELOG.md`）上验证机制，再谈 README** |
| 2 | **让谓词「改」文档** | 沿用批 16：**只报不改**（D1 写权矩阵） |
| 3 | **追溯 53 份存量档** | 沿用批 16 的 D16-2（**同类**：会造大量历史红） |
| 4 | **把「同一事实」做成语义理解** | **本批只处理「可数事实」**（计数 / 枚举 / 版本号 / SHA 这类**可机械比对**的）；**散文语义不一致仍归属人眼** |
| 5 | **动 `release-check.mjs` 的既有闸** | 那是**既有**的门（`G-常驻3` 刚抓到 P-5）；**本批是文档形状谓词，不是发布门**——两者边界见设计档 |
| 6 | **给旧档补声明** | 存量迁移另立批次（沿用批 16） |

### §5.2 登记面

| # | 级别 | 内容 |
|---|---|---|
| **R-54** | 🟡 | **「扩面」与「换判据」是两条路**：本批选后者（**US-2/US-3**），**代价是「同一事实」需要一个机械可识别的定义** ⇒ **若定义不出来，本批会退化成「多覆盖几处」**（设计档必须给出这个定义，否则本批不成立） |
| **R-55** | 🟡 | **`CHANGELOG.md` 不在 `docs/` 下**，且**它已有自己的解析器契约**（`release-check.mjs` 的 `parseChangelogCountLine` 等）⇒ **本批新增的覆盖必须与那个契约共存、不得互相打架**（**批 16 的 P-5 就是两处推导式打架**） |
| **R-56** | 🟡 | **批 16 的两次「无法从报告重建」**（实施者超时无报告 · DSH 重启清空 job store）**都由「从盘上逐点测量」化解** ⇒ **登记为正面经验**：**产物比证词可靠**；**本批的验收同样以此为准** |

---

## §6 历史行

| 日期 | 变更 |
|---|---|
| 2026-09-16 | 首版（需求待评审）：**立项来源 = 批 16 分歧审计的一句结论**（「谓词不是错的——是它的作用域与它的宣言不匹配」）· **六条实测证据 P-1…P-6**（逐条带批 16 的评审/审计出处；**5 条由独立评审发现、1 条由既有解析器发现、父侧自查 0 条**）· **★ §2.3 的结构性观察**：按面逐个扩是打地鼠 ⇒ **本批换判据形态而非多覆盖几处** · **US-1…US-6** · **N-1…N-7**（**N-1 不回归正则猜中文** · **N-2 不追溯存量** · **N-3 批 16 一条不退化**）· §5.1 **六项不做**（含 `README.md` 与 `release-check.mjs` 的边界）· §5.2 **R-54…R-56** |
