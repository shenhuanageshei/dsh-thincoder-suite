# 设计（批 28）：对外收口 —— 上游报告包 ⊕ 版本哨兵 ⊕ 结项说明

- 日期：2026-09-26
- 状态：**设计已通过评审（第 1 轮 PASS），待实现**（等用户指令派单；令牌槽每会话只有一个，须逐个「评审 → 立刻派单」）
- 来源：**平台侧三问题**（本机实测，非本仓可修）+ 批 26 的「被撞」教训（0.1.7 换契约是我们撞出来的）
- 前序：[`dsh017-batch26-design.md`](./dsh017-batch26-design.md) · [`dsh017-full-alignment-design.md`](./dsh017-full-alignment-design.md)

## §1 背景与用户故事

**三个平台侧问题（都有一手证据，但都不属本仓）**：
① `job_output` 读全文**对所有作业都崩**（含平台自产）；**干净移除 profile 自带 rc.1 副本后仍崩** ⇒ 平台自身读法缺陷（批 26 §8 的环境实验）。
② **沙箱下子进程执行会挂住**：dsh 子代理跑 `check` 命令**既不报错也不返回**（实测挂 8 分钟、日志 0 字节；宿主同命令 7.24 秒全绿）——正确行为应是**报错**（fail-closed），挂起只能靠兜底。
③ **advisor 三形态并存**：coord 超时 / main 截断 / bce 内联成功——内联路径存在但**条件不明**（批 26 的 `pathForm` 已让它**可查**，但没人系统看过）。

- **US-1**：把这三件事整理成**自包含的上游报告包**（现象 · 最小复现 · 证据 · 影响 · 建议），别人（或未来的我）拿到就能复现，不用再翻会话导出。
- **US-2**：**版本哨兵**——插件装配时读平台版本并与「已知契约面基线」对照，不匹配就**响亮警告**并指向契约矩阵 ⇒ 把「换契约被撞」变成「换契约被提示」。
- **US-3**：**契约面巡检**一条命令出一个报告（九条锁 + 平台版本 + 落盘目录状态），运维/交接时一眼看清。
- **US-4**：**结项说明**——把 24→28 的批次脉络、九条触点矩阵、护栏机制（令牌/docHash、锚、锁、宿主验收）写成常设页，供将来任何会话接手。

## §2 方案

### §2.1 上游报告包（US-1）
新增 `docs/upstream-2026-09-26-platform-issues.md`（常设档）：三条问题各含「现象 / 最小复现（含命令）/ 一手证据（文件:行 或作业 id 与字节数）/ 影响面 / 给上游的建议」；
附 `scripts/repro-platform-issues.ps1`：**只读**复现（起一个 2 秒后台作业试 `job_output`；在子代理侧跑一条无害命令看是否挂；汇总 `pathForm` 分布），输出一段可直接贴进 issue 的报告。

### §2.2 版本哨兵（US-2）
插件装配期取平台版本，**按钉死顺序取第一个可得的**：① `ctx.pkg?.version`（宿主注入）② profile manifest 的 `dsh` 依赖声明 ③ 进程可见的 `@deepseek-ai/dsh-core` 包版本；**三者皆不可得 ⇒ 标 `unknown` 并走 A28-3 的告警分支（不猜、不崩）**；读取逻辑集中在一个 `readPlatformVersion(ctx)`（单一事实源，供哨兵与巡检工具共用）；与 `lib/contract-baseline.mjs`（新增，记录「本轮验证过的平台版本 + 九条触点期望形态」）对照：
- **一致** ⇒ 静默；**不一致** ⇒ `console.warn` 响亮告警（含「未知版本」分支）+ 在设置页/工具描述里给一句指向 `docs/dsh017-*.md` 的提示。
**纪律**：哨兵**只警告不阻断**（拒启动会变成新的砖化源）。

### §2.3 契约面巡检（US-3）
新增工具 `contractWatch`（只读）：一次调用返回 `{ platformVersion, locks: [{ id, ok, evidence }…] , jobsDir: { files, bytes, oldestDays } }`；
九条锁的判定**复用** `test/platform-surface.test.mjs` 的谓词（抽成共享 helper，测试与工具同一事实源，避免第二份字面）。

### §2.4 结项说明（US-4）
新增 `docs/dsh017-program-closeout.md`：批次脉络（24/25/26/27/28）· 九条触点矩阵与「两处同改」清单 · 四道护栏（令牌 docHash · 锚/AC · 常驻锁 · 宿主验收回执）· 以后遇到平台换契约的**标准动作**（先跑巡检 → 对照矩阵 → 开新批）。

## §3 受影响文件

| 文件 | 改动 |
|---|---|
| `lib/contract-baseline.mjs` | **新增**：平台版本基线 + 九条触点期望形态（单一事实源） |
| `lib/index.mjs` | 装配期跑哨兵；注册 `contractWatch` 工具 |
| `test/platform-surface.test.mjs` | 九条谓词抽成共享 helper（测试与工具同源） |
| `test/contract-baseline.test.mjs` | **新增**：哨兵三态（一致静默 / 不一致告警 / 版本未知也不崩）· 巡检返回形状 |
| `docs/upstream-2026-09-26-platform-issues.md` · `scripts/repro-platform-issues.ps1` | **新增**：上游报告包与只读复现脚本 |
| `docs/dsh017-program-closeout.md` | **新增**：结项说明 |
| `docs/2026-09-05-defect-registry.md` · `docs/test-lifecycle.md` · `CHANGELOG.md` · `README.md` · `package.json` | 登记级联与版本 |

## §4 决策

| # | 决策 | 理由 |
|---|---|---|
| D28-1 | 哨兵**只警告不阻断** | 与 D27-1/D26-2 同一条纪律：不得成为新砖化源 |
| D28-2 | 版本基线**单独成档**（`lib/contract-baseline.mjs`） | 九条锁与哨兵共用同一事实源，避免两处各写一份 |
| D28-3 | 上游问题**只出报告与只读复现**，不改平台 | 不属本仓；改成「能一击复现」比写十段描述有用 |
| D28-4 | 巡检工具**只读**、复用测试谓词 | 工具与锁同一事实源 ⇒ 不会各自漂移 |
| D28-5 | 结项说明**进仓**（非会话内） | 会话会丢，档不会 |

## §5 锚与判据

### §5.1 机验锚
| 锚 | 检索目标 | 谓词 | 期望 |
|---|---|---|---|
| **A28-1** | `test/contract-baseline.test.mjs` | 注入「与基线一致」的版本 | 装配**零告警** |
| **A28-2** | 同上 | 注入「不一致」的版本 | 一条响亮告警，**且不阻断装配** |
| **A28-3** | 同上 | 注入「取不到版本」 | 标未知 + 告警，**不崩** |
| **A28-4** | 同上 | 调 `contractWatch` | 返回 `{platformVersion, locks(9 条), jobsDir}` 且形状稳定 |
| **A28-5** | 静态锁 | 九条谓词在测试与工具间**同源** | 不存在第二份字面（grep 唯一） |
| **A28-6** | `scripts/repro-platform-issues.ps1` | 只读性 | 不改任何配置/文件（脚本自查段断言） |

### §5.2 验收标准
| # | 验收标准 | 层 | 锚 | US |
|---|---|---|---|---|
| **AC-1** | 版本不一致被响亮提示、装配不受影响 | T1 | A28-1 · A28-2 · A28-3 | US-2 |
| **AC-2** | 巡检一次调用给出九条锁 + 版本 + 落盘目录状态 | T1 | A28-4 | US-3 |
| **AC-3** | 九条谓词单一事实源 | T2 | A28-5 | US-3 |
| **AC-4** | 上游报告包自包含（现象/复现/证据/影响/建议齐备）且脚本只读 | T2 | A28-6 | US-1 |
| **AC-5** | 结项说明进仓，含批次脉络 + 矩阵 + 四道护栏 + 标准动作 | T3 | — | US-4 |
| **AC-6** | 登记级联（台账新档 · CHANGELOG · 版本 · 事实 id） | T3 | — | 全部 |

## §6 边界（本批不做）

- **不改平台**（上游问题只出报告）；
- 不做自动升级/自动改配置（哨兵只提示，动作留给人）。

## §7 变更记录
| 日期 | 变更 |
|---|---|
| 2026-09-26 | 首版：上游报告包 · 版本哨兵 · 契约面巡检 · 结项说明；决策 D28-1…5 · 锚 A28-1…6 · AC-1…6 |
