# 需求：D1–D7 文档纪律 + 上游失败路径语义 + DOC-HYGIENE 提示词级纪律 —— 批 11

- 日期：2026-09-13
- 设计输入：[`2026-09-13-doc-discipline-consult-minutes.md`](./2026-09-13-doc-discipline-consult-minutes.md)（会诊 id 2，**4/4 交付**；含 14 条父侧实测、五问裁定、不可真机验证清单）
- 用户裁定（范围，已锁定）：① **D1–D7 成文**（逐条标定现有/部分/缺席；**D7 的「核销同步」照旧不吸收**）· ② **只收编失败路径里有实测痛的两条**（停产报告含可选项清单 + 不自动重派），**不做**批文档载体、**不做**修正轮机制 · ③ **加一条提示词级文档卫生纪律** · ④ 订正「§2 批 11 行共 4 条」· ⑤ **恢复菜单 = 五项**（上游四项 + 改道）· ⑥ **建新测试档做两项机械检查**
- 章节：§0 会诊汇总 · §1 总目标 · §2 背景与问题 · §3 用户故事 · §4 非功能标准 · §5 边界与登记面
- 状态：**设计待评审**

---

## §0 会诊汇总

**全量纪要见 [`2026-09-13-doc-discipline-consult-minutes.md`](./2026-09-13-doc-discipline-consult-minutes.md)**。

### §0.1 模型结果（**4/4 交付**——codex 在连续两次 `PROCESS_ERROR` 后首次回话）

| 模型 | 结果 | 关键贡献 |
|---|---|---|
| `deepseek-official:deepseek-v4-pro` | ✅ | 反驳 T9 前提 · **六串禁字锁** · `discipline.md` 落点论据 · 「能放进返回串的绝不放进提示词」 |
| `zai-coding-cn:glm-5.3` | ✅ | **T9 重基线结构上走不通**（授权面口径锁）· persona 与 brief 是**两个通道** · **提示词被可移植性锁封嘴 ⇒ 自含是锁强制形态** · 字节雷区 |
| `codex-cli:gpt-6-astra` | ✅ | **首条即反驳 T9** · 返回值**后处理 wrapper**（否决逐站点改 17 处）· `index.mjs:937` 的**活矛盾** · 「不能因为未重启就把活体目录当隔离验证环境」 |
| `kimi-api:kimi-k3` | ✅ | **新夹具留置旧夹具**（锚 B 只拦 M/D）· 「归档快照允许过时，常设条文不允许含快照」· AC 分两组 |

### §0.2 父侧实测（14 条，全在纪要 §1；此处只列**决定设计**的九条）

| # | 事实 |
|---|---|
| 1 | **★ 父侧任务书前提错误**：改 `lib/prompts/engineering.md` **不触动 T9**——T9 测 `buildCoderBrief` 的 **stages 缺省**输出（`test/stages.test.mjs:96`），缺省时不进 `renderStagesBlock`（`lib/eng.mjs:486-488`）；`ENGINEERING` 只在 `:24`/`:79`，不在 brief 路径 |
| 2 | **T9 重基线结构上不可行**：`AP_TEST_AUTHORIZED` 口径锁要求 `^test\/…\.test\.mjs$`，而夹具是 `.txt` |
| 3 | **★ T-PK15 六串禁字**（递归扫 `lib/**`）：`/methodology/i` · `/docs\/design/` · `/ENGINEERING-MODE/` · `check-doc-width` · `check-ledger` · `docs/TODO.md` ⇒ **新提示词不得写「METHODOLOGY」，纪律必须自含** |
| 4 | **`discipline.md` 注册进每个会话**（`lib/index.mjs:808-813`），`engineering.md` 只在工程模式 |
| 5 | **字节雷区**：失败串不得含字面 `Partial output`（`codex-runner.test.mjs:4037`） |
| 6 | **`index.mjs:937` 是活矛盾源头**：「stage 失败 ⇒ 重派一个（token 未消费）」= **现行的自动重派指令** |
| 7 | **既有失败断言全是 `includes`** ⇒ 追加式后缀不破任何断言 |
| 8 | **D1–D7 在本仓零内容**；枚举只在上游 `discipline-engineering.md:103-111`。逐条现状：**D1 缺席** · D2 已有 · D3 部分机检 · D4 部分 · D5 **机制已有、纪律未成文** · D6 部分且对象不同 · D7 一半（核销同步已废除） |
| 9 | **`lib/prompts.mjs` 顶层读十档、缺档即 throw** ⇒ 编辑**已加载档的内容**不新增启动期失败面；**新增被加载档**才会 |

### §0.3 父侧裁定（详见纪要 §2）

| # | 裁定 |
|---|---|
| **J11-1** | D1–D7 **双层形态**：规范层进 `METHODOLOGY.md` 新子节（**零状态句、零聚合计数**）；标定层**只进批 11 设计档**（表头 as-of **`0c15c69` / v0.18.0** —— **评审轮次 2 的 #2 订正**：初稿误用 `6bb02ea`，那是**其后**的文档收口提交，**不是版本点**） |
| **J11-2** | 上游关系 = **改写 + 出处行**（照抄会进口已废除名词；纯引用对发布产物不可见且重现「未定义编号」病） |
| **J11-3** | **D7 不复活废除名词**：「核销」全节**仅一处、只在否定语境**；留痕面列我方既有形态 |
| **J11-4** | D3 收编 `METHODOLOGY.md:32` 的既有 bullet（否则新子节第二天就违反 D2 自己） |
| **J11-5** | D6 覆盖**代码面 + 文档面**；判据 = **三谓词回读**（预期改动在位 / U+FFFD = 0 / 行数与预期增删一致）+ **一条禁令**（结构性改档用编辑工具，禁 shell 单行整档重写） |
| **J11-6** | D6 机械层 = **新建 `test/doc-hygiene.test.mjs`**（**用户裁定**）：U+FFFD 全仓扫描 + 五个常设档 canary（防截断） |
| **J11-7** | 失败路径**双落点**：**结构进返回串**（机锁面）· **政策进提示词**（行为面）；**能放进返回串的绝不放进提示词** |
| **J11-8** | 返回串形态 = **出口边界后处理 wrapper**，**否决逐站点改 17 处**；**不改 `deathLine`**（R0 档）；helper 须在 `stageGateNote`→`makeWriteGate` 切片**之外** |
| **J11-9** | 恢复菜单 = **五项**（上游四项 + **改道**）；**改道的必要性有实测**（codex 连续两次 `PROCESS_ERROR`，上游四项无一对症） |
| **J11-10** | **`index.mjs:937` 必须同批划界**（保留「子代理自报阶段失败 ⇒ 重派一次」的授权，加「崩溃/超时/abort 不自动重派」的边界），否则两条纪律互相矛盾 |
| **J11-11** | DOC-HYGIENE 落 **`discipline.md`**（覆盖面：所有模式都咬）· 五句带判据 · **自含**（T-PK15）· 只追加不删改（AC-V14） |
| **J11-12** | **本批零字节触碰 T9 夹具与 `prompts.mjs`**；**用户预接受的 T9 重基线成本从本批划掉** |
| **J11-13** | 本批一切 AC **只能措辞为字符串/结构断言**，**不得出现行为断言**（未重启 ⇒ 真机行为不可验） |

---

## §1 总目标（一句话）

把三条**只在上游或只在我们脑子里**的纪律**落到本仓、且形态自含可判**：D1–D7 成文（**规范进 METHODOLOGY、现状快照进设计档**）、失败停下来时**把选项摆给用户而不是自己重派**、以及一条**所有模式都会看到的文档卫生纪律**。

---

## §2 背景与问题

| # | 问题 | 根因 | 证据 |
|---|---|---|---|
| **P1** | **D1–D7 是本仓的未定义编号** | 引用 22 处、零内容 | 全仓 grep；枚举只在上游 |
| **P2** | **D1 写权矩阵完全缺席** | 从未成文 | grep `写权矩阵` = 0 |
| **P3** | **D5 已实现为机制、未写为纪律** | 批 6b 只交付了代码 | 守卫 E 在位，METHODOLOGY 无此条 |
| **P4** | **失败时没有选项可依** | 无「不自动重派」明示、无菜单、无用户裁决框架 | `lib/eng.mjs` ~17 个非成功返回，全是 detail + partial |
| **P5** | **`index.mjs:937` 是一条现行的自动重派指令** | 与新纪律直接张力 | 逐字「re-spawn ONE new eng_coder …」 |
| **P6** | **文档卫生只有散文纪律、无提醒面** | 主代理的系统提示里没有它 | `discipline.md` 只有 review-discipline |
| **P7** | **静默写坏已实证三次** | 无「写后回读」的一般规则、无机检 | PowerShell 双编码 · METHODOLOGY 被截为 1 行 · U+FFFD |

**靶心 = P1/P2/P4/P6**：纪律**在别处**，而执行者看不到；**P7 是最疼的一条**（有三次实证）。

---

## §3 用户故事

| # | 用户故事 | 判定句（可机验） |
|---|---|---|
| **US-1** | 作为**下一个会话**，我希望 `D1–D7` **不再是编号**，而是本仓可读的七条规范 | `METHODOLOGY.md` 新子节含 D1…D7 七条**现在时规范句** |
| **US-2** | 作为**评审员**，我希望规范条文里**不含经验断言**（那样它就不会漂） | 子节内**零**「现有/部分/缺席」状态词、**零**聚合计数句 |
| **US-3** | 作为**读者**，我希望「我们做到哪了」是**一份带日期的快照**，而不是会腐的现在时 | 标定表**只在批 11 设计档**，表头含 as-of 提交号，逐格带 file:line |
| **US-4** | 作为**读者**，我希望 D7 **不复活**已废除的名词 | 「核销」全节**恰一处**且只在「不吸收」句内 |
| **US-5** | 作为**执行者**，我希望子代理崩溃/超时/被 abort 时**看到选项**，而不是我只知道它失败了 | 终态失败返回的 output 含**五项菜单** + **不自动重派**声明 |
| **US-6** | 作为**用户**，我希望父代理**不要**自己重派，而是把选项摆给我 | 提示词含政策句；返回串含「由用户裁决」 |
| **US-7** | 作为**用户**，我不希望两条纪律互相矛盾 | `index.mjs:937` 保留「自报阶段失败 ⇒ 重派一次」但**划界**（崩溃/超时/abort 不重派） |
| **US-8** | 作为**主代理**，我希望文档卫生纪律**在每个会话**都能看到 | 纪律落 `discipline.md`（`systemPrompt.section` 注册面） |
| **US-9** | 作为**主代理**，我希望纪律**自含**（不指向我看不到的路径） | 新文本过 T-PK15 六串负向 grep |
| **US-10** | 作为**用户**，我希望「写入静默失败」这类事**有机械网** | `test/doc-hygiene.test.mjs` 两项检查（U+FFFD / 常设档 canary） |
| **US-11** | 作为**维护者**，我希望本批**不花**那笔用不上的授权 | T9 夹具与 `prompts.mjs` **零字节改动** |
| **US-12** | 作为**审计员**，我希望知道本批哪些主张**没法验** | 设计档含「不可真机验证清单」；AC 全是字符串/结构断言 |
| **US-13** | 作为**维护者**，我希望 `§2 批 11 行共 4 条` 这个错数被订正 | 三处全改 3；订正后再留 §6 变更理由行 |

---

## §4 非功能标准

| # | 标准 | 判定 |
|---|---|---|
| **N-1** | **零字节触碰 T9** | `test/fixtures/**` 零 diff；`buildCoderBrief` / `renderStagesBlock` 零改动；`stages.test.mjs` 不改自绿 |
| **N-2** | **启动期失败面零扩大** | `lib/prompts.mjs` 零字节；`lib/prompts/` 无新档 |
| **N-3** | **六串零命中** | 新文本过 T-PK15 负向 grep |
| **N-4** | **基线档零改动** | 测试改动**只落基线后档 + 新档**；`git ls-tree -r 2e6ca8b -- test` 为只读清单 |
| **N-5** | **登记制先行** | 新档在**落地同一 stage 内**入 T-E19 + 台账 §三（含理由与裁定引用） |
| **N-6** | **每条新断言可失败** | 交付须附变异矩阵（律 3） |
| **N-7** | **AC 无行为断言** | 全部措辞为字符串/结构断言（未重启） |
| **N-8** | **保四值词表** | `AC-V14` 绿（**评审 #11 订正**：该锁实际罩**四档**——`discipline.md` / `engineering.md` / **`lib/index.mjs`** / `README.md`；本批要改的前三档全在其中） |

---

## §5 边界与登记面

### §5.1 本批不做

| # | 不做项 | 理由 |
|---|---|---|
| 1 | **不做批文档载体**（batchDoc / 六段档） | 上游载体是 T-ONLY；用户已裁 |
| 2 | **不做修正轮机制**（`修正 N/5` 代码路径） | 上游 #1/#2；用户已裁 |
| 3 | **不强制 `docs` 参数**（上游 #9） | 用户已裁 |
| 4 | **不引入 `stalled` 终态** | status 仍 `failed`；改状态枚举影响面大 |
| 5 | **不动「失败不簿记」** | `stage-gate.test.mjs:209` 锁在 |
| 6 | **不改 `deathLine` 格式** | 被 18 例锁 + **该档是 R0 不可入授权面**；块只能**追加** |
| 7 | **不给失败路径加阶段门横幅** | `stage-gate.test.mjs:210` 反向锁在 |
| 8 | **不做一般性文档检查器** | `§5:129` 明文不吸收 + `T-PK15` 锁着那两个检查器名 |
| 9 | **不做 docs 地图双射检查** | 最贴近 §5:129 拒绝的方向；**列为将来决策项**（R-25） |
| 10 | **不新建被加载的提示词档** | 新增启动期 throw 面、零收益 |
| 11 | **不做 T9 夹具重基线** | **用不上**（J11-12），且今天结构上走不通（R-26） |

### §5.2 登记面

| # | 级别 | 内容 |
|---|---|---|
| **R-15**（沿用） | 🔵 | codex 在批 9/批 10 连续 `PROCESS_ERROR`、**本批恢复交付** ⇒ 观察是否稳定 |
| **R-23** | 🔵 | **父侧任务书前提错误**（误判改 `engineering.md` 必重基线 T9，四家一致反驳）⇒ **教训：把「某改动会碰某锁」当前提写进任务书前，必须先读那条锁的断言本身** |
| **R-24** | 🔵 | **T-PK15 六串禁字是提示词写作的隐形约束** ⇒ 我方提示词**不能指向 METHODOLOGY**，纪律必须自含；「提示词 ↔ METHODOLOGY 双源」**是锁强制形态、不是违反 D2** |
| **R-25** | 🔵 | **docs 地图登记无任何机检**（只有 `verdict-contract` 一档有逐档锁）⇒ 漏登不会红。**将来决策项** |
| **R-26** | 🔵 | **T9 重基线结构上不可行**（授权面口径锁要求 `.test.mjs`，夹具是 `.txt`）⇒ 真要改须先改宽那把元锁（**改锁中锁**），独立批次 |
| **R-27** | 🔵 | **本会话第 12 次自报计数漂移**（吸收清单三处「共 4 条」实测 3）⇒ 父侧已修两处，剩 `§2:84` 待本批订正；另两处陈旧 `:141` 行号指针已修（**恰是 D4 要治的病，且发生在同一批内**） |
| **R-28** | 🔵 | **评审员漏写 `VERDICT:` 末行 ⇒ 宿主按启发式回落到「不通过」**（轮次 3 首次触发；宿主自己在提示里说明「这不等于评审员判了不通过」）⇒ **一次真正的通过会被系统判成不通过，白烧一轮**。**对策**：派单的 `documents` 与任务语境应显式要求评审员**在末行给出 `VERDICT: PASS|FAIL`（英文关键字，不翻译）** |

---

## §6 历史行

| 日期 | 变更 |
|---|---|
| 2026-09-13 | 首版（设计待评审）：会诊 id 2 **4/4 交付** → §0 汇总 + 九条决定性实测 + **J11-1…J11-13 裁定**（含六项用户裁定）；US-1…US-13；N-1…N-8；§5.1 十一项不做 + §5.2 六项登记 |
