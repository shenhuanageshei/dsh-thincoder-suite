# 需求：会诊结果的投递与消化 —— 批 15

- 日期：2026-09-15
- **会诊纪要**：[`2026-09-15-consult-delivery-consult-minutes.md`](./2026-09-15-consult-delivery-consult-minutes.md)（**会诊 id 1，4/4 交付但仅 2 份设计级**；§1 父侧独立回盘核 · **§2 R-1…R-10 逐问裁定** · §3 T-1…T-3 待裁 → **用户已裁定「都按父侧建议」**）
- **上游调研**：`D:\workspace\thincoder` @ `58ddb27`（`v0.12.61-4-g58ddb27`）——**源实现在盘，逐条核实**（勘察子代理 `963108c5`）
- 用户裁定（已定）：**① 通知，回来后停在一个「必须向你汇报」的断点** · **② 明确目标 / 无人值守 / 明确授权时可不停** · **③ 会诊结果默认落档，特定情况可豁免** · **④ T-1 删批 14 的 A 项 · T-2 采纳 stop 产 digest 与 jobs 缺失拒发 · T-3 digest 分开报「交付数」与「有效数」**
- 章节：§0 纪要索引（**不冒充纪要**）· §1 总目标 · §2 背景与问题 · §3 用户故事 · §4 非功能标准 · §5 边界与登记面 · §6 历史行
- 状态：**需求待评审**

---

## §0 会诊与调研的索引（**本节不复制纪要内容**）

> **★ 订正（对照 `METHODOLOGY.md:76` 的流程）**：本档初版把「上游调研结论」写进了 §0，**冒充了纪要**。按规范，**纪要是独立交付物**（本仓既有 **9 份** `*-consult-minutes.md`），需求档只应**指向它**。

| 交付物 | 位置 | 内容 |
|---|---|---|
| **会诊纪要** | [`2026-09-15-consult-delivery-consult-minutes.md`](./2026-09-15-consult-delivery-consult-minutes.md) | §0 汇总（**含「4/4 交付 ≠ 4 份有效」的诚实标注**）· §1 父侧独立回盘核 · **§2 R-1…R-10 逐问裁定** · §3 T-1…T-3 · §4 五条教训 · §5 不可验清单 |
| **上游坐标** | 纪要 §0 + 设计档 §10 偏离表 | 上游 `58ddb27` · 本仓复制源 `3e1234b^` · 分叉事件 `3e1234b`（2026-09-07 04:10） |
| **勘察报告** | 会话记录（子代理 `963108c5`） | 上游源码定位 · `DESIGN-dsh-port.md` 悬空引用（X-1）· 平台能力核实 |

> **★ 一条流程教训（写进需求档因为它影响本档形态）**：**「勘察」不是「会诊」**——父侧初版派了只读子代理查事实，**把它当成了会诊**，跳过了纪要这一步。⇒ 本档与设计档**都是在纪要之后写的**（登记为 **R-44**）。

---

## §1 总目标（一句话）

**把会诊从「发起后靠调用方记得回来轮询」改成「结束时自动投递、回来后停在必须向用户汇报的断点、处置结果默认落档」**——其中前两条**照搬上游 R17 已裁定的形态**（本仓只是晚了六天没跟），第三条**是本仓要走在上游前面的新设计**。

---

## §2 背景与问题

| # | 问题 | 根因（`file:line`） | 实测 |
|---|---|---|---|
| **P1** | 会诊结束时**无人知晓、无物推动** | `lib/consult.mjs:276-308`：`startConsultSession` fire-and-forget 起子代理即返回；唯一消费面是 `checkConsultSession`（`:317-352`）的**阻塞轮询**；`lib/index.mjs:1033-1034` 的描述面**自己承认**「There is NO completion notification … you must come back yourself」 | 本会话撞 3–4 次 |
| **P2** | **协议落后上游且从未记录** | `lib/consult.mjs:2` 头注仍是三工具协议；上游 `3e1234b`（2026-09-07）已退役 check；分叉从未入档（唯一痕迹 `CHANGELOG.md:215` 还在对上游已删协议做加固 = **X-2**） | 纪要 §0.1 |
| **P3** | **重启 = 静默全灭** | `lib/state.mjs:11-13` 与 `:55-56`：`consult*` 明文**不进持久化视图**，Map 纯内存；**已实际咬过一次**（`docs/2026-09-13-design-review-guard-requirements.md:19`：重启后 `unknown consult id`，kimi-api:kimi-k3 不可复得） | 有实况登记 |
| **P4** | **完成通知只含一行指针** | 平台契约（`lib/advisor.mjs:1677-1679` 登记）：「完成通知只含一行指针（**全文经 `job_output` 读取**，保尾截断）」⇒ **digest 的实际阅读动作是 wakeup 回合里再调一次 `job_output`**，而父侧初版的设计**完全没写这一步** | 上游/平台双证 |

**靶心 = P2**；**P3/P4 是两家会诊各自独立点出、父侧初版全部漏掉的**。

---

## §3 用户故事

| # | 用户故事 | 判定句 | 可验层 |
|---|---|---|---|
| **US-1** | 作为**主代理**，我希望会诊**结束时自动收到通知**，不用记得回来轮询 | 全 settle 时**产生一次投递**（忙时注下一步 / 空闲开回合），**调用方零动作** | **T2** |
| **US-2** | 作为**用户**，我希望主代理拿到结果后**停下来向我汇报** | 投递被消费的回合**产出一份面向我的汇报**（采纳/不采纳/理由），**然后停** | **T3**（质量人眼）+ T1（形状） |
| **US-3** | 作为**用户**，我希望在**已明确目标 / 无人值守 / 已明确授权**时不必每次停 | 存在**显式豁免**（缺省=停）；豁免**留痕** | **T1** |
| **US-4** | 作为**审计员**，我希望**每条意见被分类处置**，不采纳须给理由 | 纪要里**每条意见恰一条处置**，不采纳**必有理由** | **T3**（人眼） |
| **US-5** | 作为**下一批的维护者**，我希望纪要**默认落盘** | 缺省 = 写出纪要档；豁免须**显式且留痕** | **T2** |
| **US-6** | 作为**维护者**，我希望本仓 consult 协议**与上游对齐或有据偏离** | **六列偏离表**齐备，含**方向四值**（含「上游已改本仓未跟」） | **T2** |
| **US-7** | 作为**调用方**，我希望 `consult_check` 去留**明确**，不留半套协议 | 二选一 + **描述面与注册点同步清零** | **T2** |
| **US-8** | 作为**审计员**，我希望悬空引用不再指向不存在的档 | **X-1** 被处置（**归批 14**） | T2 |
| **US-9** | 作为**审计员**，我希望**未被消化的会诊拦得住下一次发起** | **门禁不变量**：不存在 `settled ∧ ¬digested ∧ ¬minutesExempt` 的会话 | **T1** |
| **US-10** | 作为**用户**，我希望**被停掉的会诊不丢已收到的回复** | `stopped` 会话**产墓碑 digest**（含 stop 死亡行） | **T1** |
| **US-11** | 作为**审计员**，我希望 digest 头部行**分开报交付数与有效数** | 头部行含 `N of M replied` **且** 有效数（T-3） | **T1** |
| **US-12** | 作为**调用方**，我希望在 wakeup 回合**知道要先读全文** | `prompts/main.md` 含「先 `job_output` 读全文，再处置」 | **T2** |

**★ US-9…US-12 是纪要 R-10 / R-3 / T-3 / P4 带来的新增**——**父侧初版一个都没有**。

---

## §4 非功能标准

| # | 标准 | 判定 |
|---|---|---|
| **N-1** | **平台已有等价能力则不自造** | DSH `dsh-tool-jobs` 已提供「injected into a busy owner's next step, or opening a turn on an idle one」（`lib/index.js:10-12`）⇒ **不复刻上游的 `_pendingAsyncResults`** |
| **N-2** | **消费链 fail-closed，辅助面 fail-open** | 纪要 R-4 的裁定（start 面拒发 / 记录面 fail-open） |
| **N-3** | **每条 AC 必须打可验性三层标（T1/T2/T3）** | **纪要 R-10**；**不加分层 ⇒ 交付评审会放过虚构的绿** |
| **N-4** | **交付必须在平台墙钟内完成** | dsh 子代理同步调用活不过 600s（`eng.mjs:914-916`）⇒ 实施须 `background: true` 或拆 stages |
| **N-5** | **零新增依赖** · **不新增测试档** · **不新增顶层 `test(`** | 台账 §三零改（批 13 先例） |
| **N-6** | **`lib/**` 需重启才生效**——须在交接页登记 | 与前两批同类 |
| **N-7** | **不擅自重启 DSH、不擅自改用户配置** | 常设约束 |

---

## §5 边界与登记面

### §5.1 本批不做

| # | 不做项 | 理由 |
|---|---|---|
| 1 | **不照搬上游的 `_pendingAsyncResults`** | 平台已有等价设施（N-1） |
| 2 | **不改 consult 只读子代理语义** | 上游明文 |
| 3 | **不做模型间交叉通信** | 上游明文列为不做 |
| 4 | **不做部分 settle 提前注入** | 上游已裁「全 settle 才入流」 |
| 5 | **不修 X-1（悬空引用）** | 归批 14（同物种） |
| 6 | **不做 `escapeXml`** | **纪要 R-5 裁定**：DSH 的 `job_output` 是纯文本、无 XML 信封——**有意不跟，进偏离表** |
| 7 | **不新增持久化**（`consultSessions` 保持纯内存） | 纪要 R-5：**没有持久化就没有迁移面，写迁移表就是写假事实**；取证靠**新台账** |
| 8 | **不给「汇报质量」加机检锁** | 语义只能人眼（R-41）；**但中间层要加**（US-9 的门禁不变量） |

### §5.2 登记面

| # | 级别 | 内容 |
|---|---|---|
| **R-40** | 🟡 | **上游分叉未记录**——本仓停在 pre-R17 而**从未记下上游 `3e1234b` 改过 consult**。⇒ **教训**：**移植是抄一个时间点，而源会继续走**；移植物**必须记下被抄的坐标**。**建议上升为常设纪律**（与 D4 指针纪律并列） |
| **R-41** | 🔵 | **「汇总质量」无机器强校验**——机械只保**形式**（每会话被消化过 / 每条意见有处置行），**语义正确性只能人眼**。**不许超卖** |
| **R-42** | 🔵 | **`wait_for "consult done"` 本仓没有**——上游有此原语。本批走平台 job 后由 `job_output` 承担 ⇒ **不新增工具**（纪要 R-2 的 FR-3） |
| **R-43** | 🟡 | **会诊的「交付数」与「有效数」是两个数**——本批会诊自身即为证（**4/4 交付，2 份设计级**：1 份答错问题、1 份简报回声）。⇒ **digest 头部行须分开报**（US-11），且**父侧今后登记会诊结果必须分开记**。**本会话新长出的一条纪律** |
| **R-44** | 🟡 | **「勘察 ≠ 会诊」**——两者是不同工序（一个查事实、一个出裁定），**跳过任一步都会使下游文档失去依据**。父侧在本批亲身犯过（派了勘察就当作会诊）⇒ **登记为流程纪律** |
| **★ R-45** | 🟡 | **「按行锚做编辑」在本会话已犯七次**（吞掉相邻行 / 留孤儿尾 / 造成半份重复文档）——**且七次全部由「程序数一遍」发现，零次由父侧读出来发现**。⇒ **对策必须是机械的**：**任何行锚替换后立刻跑计数核对**；**大段改写一律整档重写，不用行锚**。**本会话最强的一条操作纪律** |

---

## §6 历史行

| 日期 | 变更 |
|---|---|
| 2026-09-15 | 首版（需求待评审）：用户三项裁定 + 上游调研 ⇒ §0.1 溯源链 · §0.2 上游三条链 · §0.3 两处独立问题 · **US-1…US-8** · **N-1…N-6** · §5.1 七项不做 · §5.2 **R-40…R-42** |
| 2026-09-15 | **重写（用户质问「按规范合格吗」+ 纪要已落）**：① **§0 改为纪要索引**（**订正：初版把调研塞进 §0 冒充纪要**）· ② **§2 每条补 `file:line`**（初版几乎没有）· ③ **US-1…US-12 每条加可验性层标**（初版无）· ④ **新增 US-9…US-12**（门禁不变量 / 墓碑 digest / 交付数与有效数 / 先读全文——**父侧初版全无**）· ⑤ **N-3 新增「每条 AC 必须打 T1/T2/T3」**· ⑥ **§5.1 扩到八项** · ⑦ **新增 R-43/R-44/R-45**。**用户裁定 T-1/T-2/T-3 全部采纳** |
| 2026-09-15 | **整档重写（父侧第七次行锚事故的自纠）**：上一次编辑用一个跨段长锚，**结果把新内容追加在旧内容之前，造成「两套 §0–§6」的半份重复文档**（220 行、每节标题出现两次）。⇒ **改用整档重写**（不用行锚）⇒ **本档为修复后版本**。**登记为 R-45** |
